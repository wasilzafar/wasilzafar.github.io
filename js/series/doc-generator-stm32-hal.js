/**
 * doc-generator-stm32-hal.js
 * Document generation methods for the STM32 Unleashed: HAL Driver Development series.
 * Extends DocGenerator (doc-generator-core.js) via Object.assign.
 * 18 document types × 4 formats = 72 methods.
 *
 * @author wasilzafar.com
 */

Object.assign(DocGenerator, {

    // ============================================================
    // 1. STM32 PROJECT CONFIGURATION  (Part 1)
    // ============================================================

    generateStm32ProjectConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Project Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'STM32 Series: ' + (data.stm32Series || 'N/A'),
                'Cortex-M Core: ' + (data.cortexCore || 'N/A'),
                'Development Environment: ' + (data.devEnvironment || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Hardware Configuration', content: [
                'Flash & SRAM: ' + (data.flashSram || 'N/A'),
                'Clock Configuration: ' + (data.clockConfig || 'N/A')
            ]},
            { heading: '2. Peripherals', content: (data.peripherals || 'Not specified').split('\n') },
            { heading: '3. Project Goals', content: (data.projectGoals || 'Not specified').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Project Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32ProjectConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 Project Configuration'],
            ['Generated by wasilzafar.com STM32 Unleashed Series'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['STM32 Series', data.stm32Series || ''],
            ['Cortex-M Core', data.cortexCore || ''],
            ['Development Environment', data.devEnvironment || ''],
            ['Flash & SRAM', data.flashSram || ''],
            ['Clock Configuration', data.clockConfig || ''],
            ['Peripherals', data.peripherals || ''],
            ['Project Goals', data.projectGoals || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 26 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'STM32 Project');
        XLSX.writeFile(wb, (filename || 'stm32-project') + '.xlsx');
    },

    generateStm32ProjectConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 PROJECT CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.stm32Series || ''), size: 12 },
            { text: 'Core: ' + (data.cortexCore || 'N/A') + '  |  Env: ' + (data.devEnvironment || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── HARDWARE CONFIGURATION ──', size: 14, bold: true },
            { text: 'Flash & SRAM: ' + (data.flashSram || 'N/A'), size: 10 },
            { text: 'Clock Config: ' + (data.clockConfig || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── PERIPHERALS ──', size: 14, bold: true },
            { text: data.peripherals || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PROJECT GOALS ──', size: 14, bold: true },
            { text: data.projectGoals || 'Not specified', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Project Configuration', lines: lines });
    },

    generateStm32ProjectConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 Project Configuration',
            subtitle: (data.stm32Series || '') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Project Overview', content: 'Series: ' + (data.stm32Series || 'N/A') + '\nCore: ' + (data.cortexCore || 'N/A') + '\nEnv: ' + (data.devEnvironment || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Hardware Configuration', content: 'Flash/SRAM: ' + (data.flashSram || 'N/A') + '\nClock: ' + (data.clockConfig || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Peripherals', content: data.peripherals || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Project Goals', content: data.projectGoals || 'Not specified', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 2. STM32 GPIO CONFIGURATION  (Part 2)
    // ============================================================

    generateStm32GpioConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 GPIO Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Board / Target: ' + (data.board || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. GPIO Outputs', content: (data.gpioOutputs || 'Not specified').split('\n') },
            { heading: '2. GPIO Inputs', content: (data.gpioInputs || 'Not specified').split('\n') },
            { heading: '3. Debounce Strategy', content: (data.debounceStrategy || 'Not specified').split('\n') },
            { heading: '4. GPIO Speed & Drive', content: [
                'GPIO Speed: ' + (data.gpioSpeed || 'N/A')
            ]},
            { heading: '5. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 GPIO Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32GpioConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 GPIO Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Board / Target', data.board || ''],
            ['GPIO Outputs', data.gpioOutputs || ''],
            ['GPIO Inputs', data.gpioInputs || ''],
            ['Debounce Strategy', data.debounceStrategy || ''],
            ['GPIO Speed', data.gpioSpeed || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 22 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'GPIO Config');
        XLSX.writeFile(wb, (filename || 'stm32-gpio') + '.xlsx');
    },

    generateStm32GpioConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 GPIO CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Board: ' + (data.board || 'N/A'), size: 12 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── GPIO OUTPUTS ──', size: 14, bold: true },
            { text: data.gpioOutputs || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── GPIO INPUTS ──', size: 14, bold: true },
            { text: data.gpioInputs || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── DEBOUNCE STRATEGY ──', size: 14, bold: true },
            { text: data.debounceStrategy || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── GPIO SPEED / NOTES ──', size: 14, bold: true },
            { text: 'Speed: ' + (data.gpioSpeed || 'N/A'), size: 10 },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 GPIO Configuration', lines: lines });
    },

    generateStm32GpioConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 GPIO Configuration',
            subtitle: 'Board: ' + (data.board || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'GPIO Outputs', content: data.gpioOutputs || 'Not specified', color: DocStyles.colors.navy },
                { heading: 'GPIO Inputs', content: data.gpioInputs || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Debounce Strategy', content: data.debounceStrategy || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Speed & Notes', content: 'GPIO Speed: ' + (data.gpioSpeed || 'N/A') + '\n' + (data.notes || ''), color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 3. STM32 UART DESIGN  (Part 3)
    // ============================================================

    generateStm32UartDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 UART Communication Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'UART Instance: ' + (data.uartInstance || 'N/A'),
                'Baud Rate: ' + (data.baudRate || 'N/A'),
                'Transfer Mode: ' + (data.transferMode || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Buffer Configuration', content: [
                'RX Buffer Size: ' + (data.rxBufferSize || 'N/A'),
                'TX Buffer Size: ' + (data.txBufferSize || 'N/A')
            ]},
            { heading: '2. Protocol / Frame Format', content: (data.protocol || 'Not specified').split('\n') },
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 UART Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32UartDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 UART Communication Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['UART Instance', data.uartInstance || ''],
            ['Baud Rate', data.baudRate || ''],
            ['Transfer Mode', data.transferMode || ''],
            ['RX Buffer Size', data.rxBufferSize || ''],
            ['TX Buffer Size', data.txBufferSize || ''],
            ['Protocol / Frame', data.protocol || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 22 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'UART Design');
        XLSX.writeFile(wb, (filename || 'stm32-uart') + '.xlsx');
    },

    generateStm32UartDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 UART COMMUNICATION DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.uartInstance || ''), size: 12 },
            { text: 'Baud Rate: ' + (data.baudRate || 'N/A') + '  |  Mode: ' + (data.transferMode || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── BUFFER CONFIGURATION ──', size: 14, bold: true },
            { text: 'RX: ' + (data.rxBufferSize || 'N/A') + '  |  TX: ' + (data.txBufferSize || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── PROTOCOL / FRAME FORMAT ──', size: 14, bold: true },
            { text: data.protocol || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 UART Design', lines: lines });
    },

    generateStm32UartDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 UART Communication Design',
            subtitle: (data.uartInstance || '') + '  @  ' + (data.baudRate || '') + ' baud',
            perSlide: 2,
            sections: [
                { heading: 'UART Overview', content: 'Instance: ' + (data.uartInstance || 'N/A') + '\nBaud: ' + (data.baudRate || 'N/A') + '\nMode: ' + (data.transferMode || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Buffer Configuration', content: 'RX Buffer: ' + (data.rxBufferSize || 'N/A') + '\nTX Buffer: ' + (data.txBufferSize || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Protocol / Frame Format', content: data.protocol || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 4. STM32 TIMER CONFIGURATION  (Part 4)
    // ============================================================

    generateStm32TimerConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Timer Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Timer Instance: ' + (data.timerInstance || 'N/A'),
                'Timer Mode: ' + (data.timerMode || 'N/A'),
                'Timer Clock: ' + (data.timerClock || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Counter Settings', content: [
                'Prescaler: ' + (data.prescaler || 'N/A'),
                'Auto-Reload Register: ' + (data.autoReload || 'N/A')
            ]},
            { heading: '2. Channel Configuration', content: (data.channels || 'Not specified').split('\n') },
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Timer Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32TimerConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 Timer Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Timer Instance', data.timerInstance || ''],
            ['Timer Mode', data.timerMode || ''],
            ['Timer Clock', data.timerClock || ''],
            ['Prescaler', data.prescaler || ''],
            ['Auto-Reload Register', data.autoReload || ''],
            ['Channel Configuration', data.channels || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Timer Config');
        XLSX.writeFile(wb, (filename || 'stm32-timer') + '.xlsx');
    },

    generateStm32TimerConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 TIMER CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.timerInstance || ''), size: 12 },
            { text: 'Mode: ' + (data.timerMode || 'N/A') + '  |  Clock: ' + (data.timerClock || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── COUNTER SETTINGS ──', size: 14, bold: true },
            { text: 'Prescaler: ' + (data.prescaler || 'N/A'), size: 10 },
            { text: 'Auto-Reload: ' + (data.autoReload || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── CHANNEL CONFIGURATION ──', size: 14, bold: true },
            { text: data.channels || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Timer Configuration', lines: lines });
    },

    generateStm32TimerConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 Timer Configuration',
            subtitle: (data.timerInstance || '') + '  |  ' + (data.timerMode || ''),
            perSlide: 2,
            sections: [
                { heading: 'Timer Overview', content: 'Instance: ' + (data.timerInstance || 'N/A') + '\nMode: ' + (data.timerMode || 'N/A') + '\nClock: ' + (data.timerClock || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Counter Settings', content: 'Prescaler: ' + (data.prescaler || 'N/A') + '\nARR: ' + (data.autoReload || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Channel Configuration', content: data.channels || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 5. STM32 ADC CONFIGURATION  (Part 5)
    // ============================================================

    generateStm32AdcConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 ADC & DAC Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'ADC Instance: ' + (data.adcInstance || 'N/A'),
                'Resolution: ' + (data.resolution || 'N/A'),
                'Conversion Mode: ' + (data.conversionMode || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. ADC Channels', content: (data.channels || 'Not specified').split('\n') },
            { heading: '2. Analog Watchdog', content: (data.watchdogConfig || 'Not specified').split('\n') },
            { heading: '3. DAC Configuration', content: [
                'DAC Enabled: ' + (data.dacEnabled || 'N/A'),
                'DAC Waveform: ' + (data.dacWaveform || 'N/A')
            ]},
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 ADC Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32AdcConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 ADC & DAC Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['ADC Instance', data.adcInstance || ''],
            ['Resolution', data.resolution || ''],
            ['Conversion Mode', data.conversionMode || ''],
            ['ADC Channels', data.channels || ''],
            ['Analog Watchdog', data.watchdogConfig || ''],
            ['DAC Enabled', data.dacEnabled || ''],
            ['DAC Waveform', data.dacWaveform || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 22 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'ADC Config');
        XLSX.writeFile(wb, (filename || 'stm32-adc') + '.xlsx');
    },

    generateStm32AdcConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 ADC & DAC CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.adcInstance || ''), size: 12 },
            { text: 'Resolution: ' + (data.resolution || 'N/A') + '  |  Mode: ' + (data.conversionMode || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── ADC CHANNELS ──', size: 14, bold: true },
            { text: data.channels || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── ANALOG WATCHDOG ──', size: 14, bold: true },
            { text: data.watchdogConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── DAC CONFIGURATION ──', size: 14, bold: true },
            { text: 'DAC Enabled: ' + (data.dacEnabled || 'N/A') + '  |  Waveform: ' + (data.dacWaveform || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 ADC & DAC Configuration', lines: lines });
    },

    generateStm32AdcConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 ADC & DAC Configuration',
            subtitle: (data.adcInstance || '') + '  |  ' + (data.resolution || ''),
            perSlide: 2,
            sections: [
                { heading: 'ADC Overview', content: 'Instance: ' + (data.adcInstance || 'N/A') + '\nResolution: ' + (data.resolution || 'N/A') + '\nMode: ' + (data.conversionMode || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'ADC Channels', content: data.channels || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Analog Watchdog', content: data.watchdogConfig || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'DAC Configuration', content: 'Enabled: ' + (data.dacEnabled || 'N/A') + '\nWaveform: ' + (data.dacWaveform || 'N/A') + '\n' + (data.notes || ''), color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 6. STM32 SPI DESIGN  (Part 6)
    // ============================================================

    generateStm32SpiDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 SPI Protocol Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'SPI Instance: ' + (data.spiInstance || 'N/A'),
                'SPI Mode: ' + (data.spiMode || 'N/A'),
                'Baud Rate: ' + (data.baudRate || 'N/A'),
                'Transfer Mode: ' + (data.transferMode || 'N/A'),
                'Data Width: ' + (data.dataWidth || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Connected Devices', content: (data.devices || 'Not specified').split('\n') },
            { heading: '2. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 SPI Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32SpiDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 SPI Protocol Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['SPI Instance', data.spiInstance || ''],
            ['SPI Mode', data.spiMode || ''],
            ['Baud Rate', data.baudRate || ''],
            ['Transfer Mode', data.transferMode || ''],
            ['Data Width', data.dataWidth || ''],
            ['Connected Devices', data.devices || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 22 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'SPI Design');
        XLSX.writeFile(wb, (filename || 'stm32-spi') + '.xlsx');
    },

    generateStm32SpiDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 SPI PROTOCOL DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.spiInstance || ''), size: 12 },
            { text: 'Mode: ' + (data.spiMode || 'N/A') + '  |  Baud: ' + (data.baudRate || 'N/A') + '  |  Width: ' + (data.dataWidth || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── TRANSFER CONFIGURATION ──', size: 14, bold: true },
            { text: 'Transfer Mode: ' + (data.transferMode || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── CONNECTED DEVICES ──', size: 14, bold: true },
            { text: data.devices || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 SPI Protocol Design', lines: lines });
    },

    generateStm32SpiDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 SPI Protocol Design',
            subtitle: (data.spiInstance || '') + '  |  ' + (data.spiMode || ''),
            perSlide: 2,
            sections: [
                { heading: 'SPI Overview', content: 'Instance: ' + (data.spiInstance || 'N/A') + '\nMode: ' + (data.spiMode || 'N/A') + '\nBaud: ' + (data.baudRate || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Transfer Configuration', content: 'Mode: ' + (data.transferMode || 'N/A') + '\nData Width: ' + (data.dataWidth || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Connected Devices', content: data.devices || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 7. STM32 I2C DESIGN  (Part 7)
    // ============================================================

    generateStm32I2cDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 I2C Protocol Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'I2C Instance: ' + (data.i2cInstance || 'N/A'),
                'Speed: ' + (data.speed || 'N/A'),
                'Transfer Mode: ' + (data.transferMode || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Connected Devices', content: (data.devices || 'Not specified').split('\n') },
            { heading: '2. Pull-up Configuration', content: (data.pullups || 'Not specified').split('\n') },
            { heading: '3. Error Handling Strategy', content: (data.errorHandling || 'Not specified').split('\n') },
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 I2C Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32I2cDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 I2C Protocol Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['I2C Instance', data.i2cInstance || ''],
            ['Speed', data.speed || ''],
            ['Transfer Mode', data.transferMode || ''],
            ['Connected Devices', data.devices || ''],
            ['Pull-up Configuration', data.pullups || ''],
            ['Error Handling', data.errorHandling || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'I2C Design');
        XLSX.writeFile(wb, (filename || 'stm32-i2c') + '.xlsx');
    },

    generateStm32I2cDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 I2C PROTOCOL DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.i2cInstance || ''), size: 12 },
            { text: 'Speed: ' + (data.speed || 'N/A') + '  |  Mode: ' + (data.transferMode || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── CONNECTED DEVICES ──', size: 14, bold: true },
            { text: data.devices || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PULL-UP CONFIGURATION ──', size: 14, bold: true },
            { text: data.pullups || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── ERROR HANDLING ──', size: 14, bold: true },
            { text: data.errorHandling || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 I2C Protocol Design', lines: lines });
    },

    generateStm32I2cDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 I2C Protocol Design',
            subtitle: (data.i2cInstance || '') + '  |  ' + (data.speed || ''),
            perSlide: 2,
            sections: [
                { heading: 'I2C Overview', content: 'Instance: ' + (data.i2cInstance || 'N/A') + '\nSpeed: ' + (data.speed || 'N/A') + '\nMode: ' + (data.transferMode || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Connected Devices', content: data.devices || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Pull-up & Error Handling', content: 'Pull-ups: ' + (data.pullups || 'N/A') + '\n' + (data.errorHandling || ''), color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 8. STM32 DMA CONFIGURATION  (Part 8)
    // ============================================================

    generateStm32DmaConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 DMA & Memory Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'DMA Controller: ' + (data.dmaController || 'N/A'),
                'DMA Mode: ' + (data.dmaMode || 'N/A'),
                'Priority: ' + (data.priority || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. DMA Streams / Channels', content: (data.streams || 'Not specified').split('\n') },
            { heading: '2. FIFO Configuration', content: (data.fifoMode || 'Not specified').split('\n') },
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 DMA Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32DmaConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 DMA & Memory Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['DMA Controller', data.dmaController || ''],
            ['DMA Mode', data.dmaMode || ''],
            ['Priority', data.priority || ''],
            ['DMA Streams / Channels', data.streams || ''],
            ['FIFO Mode', data.fifoMode || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'DMA Config');
        XLSX.writeFile(wb, (filename || 'stm32-dma') + '.xlsx');
    },

    generateStm32DmaConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 DMA & MEMORY CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.dmaController || ''), size: 12 },
            { text: 'Mode: ' + (data.dmaMode || 'N/A') + '  |  Priority: ' + (data.priority || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── DMA STREAMS / CHANNELS ──', size: 14, bold: true },
            { text: data.streams || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FIFO CONFIGURATION ──', size: 14, bold: true },
            { text: data.fifoMode || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 DMA Configuration', lines: lines });
    },

    generateStm32DmaConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 DMA & Memory Configuration',
            subtitle: (data.dmaController || '') + '  |  ' + (data.dmaMode || ''),
            perSlide: 2,
            sections: [
                { heading: 'DMA Overview', content: 'Controller: ' + (data.dmaController || 'N/A') + '\nMode: ' + (data.dmaMode || 'N/A') + '\nPriority: ' + (data.priority || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Streams / Channels', content: data.streams || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'FIFO Configuration', content: data.fifoMode || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 9. STM32 NVIC CONFIGURATION  (Part 9)
    // ============================================================

    generateStm32NvicConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Interrupt Management & NVIC Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Priority Group: ' + (data.priorityGroup || 'N/A'),
                'Latency Budget: ' + (data.latencyBudget || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. IRQ Table', content: (data.irqTable || 'Not specified').split('\n') },
            { heading: '2. Critical Sections Strategy', content: (data.criticalSections || 'Not specified').split('\n') },
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 NVIC Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32NvicConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 NVIC Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Priority Group', data.priorityGroup || ''],
            ['IRQ Table', data.irqTable || ''],
            ['Critical Sections', data.criticalSections || ''],
            ['Latency Budget', data.latencyBudget || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'NVIC Config');
        XLSX.writeFile(wb, (filename || 'stm32-nvic') + '.xlsx');
    },

    generateStm32NvicConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 NVIC CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Priority Group: ' + (data.priorityGroup || 'N/A'), size: 12 },
            { text: 'Latency Budget: ' + (data.latencyBudget || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── IRQ TABLE ──', size: 14, bold: true },
            { text: data.irqTable || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── CRITICAL SECTIONS ──', size: 14, bold: true },
            { text: data.criticalSections || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 NVIC Configuration', lines: lines });
    },

    generateStm32NvicConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 NVIC Configuration',
            subtitle: 'Priority Group: ' + (data.priorityGroup || 'N/A'),
            perSlide: 2,
            sections: [
                { heading: 'NVIC Overview', content: 'Priority Group: ' + (data.priorityGroup || 'N/A') + '\nLatency Budget: ' + (data.latencyBudget || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'IRQ Table', content: data.irqTable || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Critical Sections', content: data.criticalSections || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 10. STM32 LOW-POWER CONFIGURATION  (Part 10)
    // ============================================================

    generateStm32LowPowerConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Low-Power Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Sleep Mode: ' + (data.sleepMode || 'N/A'),
                'Wakeup Source: ' + (data.wakeupSource || 'N/A'),
                'Voltage Scale: ' + (data.voltageScale || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Active Peripherals in LP', content: (data.activePeripherals || 'Not specified').split('\n') },
            { heading: '2. Power Budget', content: [
                'Current Target: ' + (data.currentTarget || 'N/A'),
                'Battery Capacity: ' + (data.batteryCapacity || 'N/A'),
                'Life Target: ' + (data.lifeTarget || 'N/A')
            ]},
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Low-Power Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32LowPowerConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 Low-Power Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Sleep Mode', data.sleepMode || ''],
            ['Wakeup Source', data.wakeupSource || ''],
            ['Voltage Scale', data.voltageScale || ''],
            ['Active Peripherals', data.activePeripherals || ''],
            ['Current Target', data.currentTarget || ''],
            ['Battery Capacity', data.batteryCapacity || ''],
            ['Life Target', data.lifeTarget || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Low Power Config');
        XLSX.writeFile(wb, (filename || 'stm32-lowpower') + '.xlsx');
    },

    generateStm32LowPowerConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 LOW-POWER CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Mode: ' + (data.sleepMode || 'N/A'), size: 12 },
            { text: 'Wakeup: ' + (data.wakeupSource || 'N/A') + '  |  Vscale: ' + (data.voltageScale || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── ACTIVE PERIPHERALS IN LP ──', size: 14, bold: true },
            { text: data.activePeripherals || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── POWER BUDGET ──', size: 14, bold: true },
            { text: 'Current Target: ' + (data.currentTarget || 'N/A'), size: 10 },
            { text: 'Battery: ' + (data.batteryCapacity || 'N/A') + '  |  Life: ' + (data.lifeTarget || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Low-Power Configuration', lines: lines });
    },

    generateStm32LowPowerConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 Low-Power Configuration',
            subtitle: 'Mode: ' + (data.sleepMode || 'N/A') + '  |  Wakeup: ' + (data.wakeupSource || 'N/A'),
            perSlide: 2,
            sections: [
                { heading: 'LP Mode Overview', content: 'Mode: ' + (data.sleepMode || 'N/A') + '\nWakeup: ' + (data.wakeupSource || 'N/A') + '\nVoltage Scale: ' + (data.voltageScale || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Active Peripherals in LP', content: data.activePeripherals || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Power Budget', content: 'Target: ' + (data.currentTarget || 'N/A') + '\nBattery: ' + (data.batteryCapacity || 'N/A') + '\nLife: ' + (data.lifeTarget || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 11. STM32 RTC CONFIGURATION  (Part 11)
    // ============================================================

    generateStm32RtcConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 RTC & Calendar Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Clock Source: ' + (data.clockSource || 'N/A'),
                'Calibration (ppm): ' + (data.calibrationPpm || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Alarm Configuration', content: [
                'Alarm A: ' + (data.alarmA || 'N/A'),
                'Alarm B: ' + (data.alarmB || 'N/A')
            ]},
            { heading: '2. Backup Registers', content: (data.backupRegisters || 'Not specified').split('\n') },
            { heading: '3. Tamper Configuration', content: (data.tamperConfig || 'Not specified').split('\n') },
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 RTC Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32RtcConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 RTC & Calendar Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Clock Source', data.clockSource || ''],
            ['Alarm A', data.alarmA || ''],
            ['Alarm B', data.alarmB || ''],
            ['Backup Registers', data.backupRegisters || ''],
            ['Tamper Configuration', data.tamperConfig || ''],
            ['Calibration (ppm)', data.calibrationPpm || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'RTC Config');
        XLSX.writeFile(wb, (filename || 'stm32-rtc') + '.xlsx');
    },

    generateStm32RtcConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 RTC & CALENDAR CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Clock: ' + (data.clockSource || 'N/A'), size: 12 },
            { text: 'Calibration: ' + (data.calibrationPpm || 'N/A') + ' ppm', size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── ALARMS ──', size: 14, bold: true },
            { text: 'Alarm A: ' + (data.alarmA || 'N/A'), size: 10 },
            { text: 'Alarm B: ' + (data.alarmB || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── BACKUP REGISTERS ──', size: 14, bold: true },
            { text: data.backupRegisters || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── TAMPER CONFIGURATION ──', size: 14, bold: true },
            { text: data.tamperConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 RTC Configuration', lines: lines });
    },

    generateStm32RtcConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 RTC & Calendar Configuration',
            subtitle: 'Clock Source: ' + (data.clockSource || 'N/A'),
            perSlide: 2,
            sections: [
                { heading: 'RTC Overview', content: 'Clock: ' + (data.clockSource || 'N/A') + '\nCalibration: ' + (data.calibrationPpm || 'N/A') + ' ppm', color: DocStyles.colors.navy },
                { heading: 'Alarm Configuration', content: 'Alarm A: ' + (data.alarmA || 'N/A') + '\nAlarm B: ' + (data.alarmB || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Backup Registers & Tamper', content: (data.backupRegisters || 'N/A') + '\n' + (data.tamperConfig || ''), color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 12. STM32 CAN DESIGN  (Part 12)
    // ============================================================

    generateStm32CanDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 CAN Bus Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'CAN Instance: ' + (data.canInstance || 'N/A'),
                'Bit Rate: ' + (data.bitRate || 'N/A'),
                'CAN Mode: ' + (data.canMode || 'N/A'),
                'Node ID: ' + (data.nodeId || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Filter Configuration', content: (data.filterConfig || 'Not specified').split('\n') },
            { heading: '2. Message Map', content: (data.messageMap || 'Not specified').split('\n') },
            { heading: '3. Error Handling', content: (data.errorHandling || 'Not specified').split('\n') },
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 CAN Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32CanDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 CAN Bus Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['CAN Instance', data.canInstance || ''],
            ['Bit Rate', data.bitRate || ''],
            ['CAN Mode', data.canMode || ''],
            ['Filter Configuration', data.filterConfig || ''],
            ['Message Map', data.messageMap || ''],
            ['Node ID', data.nodeId || ''],
            ['Error Handling', data.errorHandling || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'CAN Design');
        XLSX.writeFile(wb, (filename || 'stm32-can') + '.xlsx');
    },

    generateStm32CanDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 CAN BUS DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.canInstance || ''), size: 12 },
            { text: 'Bit Rate: ' + (data.bitRate || 'N/A') + '  |  Mode: ' + (data.canMode || 'N/A') + '  |  Node: ' + (data.nodeId || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── FILTER CONFIGURATION ──', size: 14, bold: true },
            { text: data.filterConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── MESSAGE MAP ──', size: 14, bold: true },
            { text: data.messageMap || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── ERROR HANDLING ──', size: 14, bold: true },
            { text: data.errorHandling || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 CAN Bus Design', lines: lines });
    },

    generateStm32CanDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 CAN Bus Design',
            subtitle: (data.canInstance || '') + '  |  ' + (data.bitRate || ''),
            perSlide: 2,
            sections: [
                { heading: 'CAN Overview', content: 'Instance: ' + (data.canInstance || 'N/A') + '\nBit Rate: ' + (data.bitRate || 'N/A') + '\nMode: ' + (data.canMode || 'N/A') + '\nNode ID: ' + (data.nodeId || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Filter Configuration', content: data.filterConfig || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Message Map', content: data.messageMap || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Error Handling & Notes', content: (data.errorHandling || 'N/A') + '\n' + (data.notes || ''), color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 13. STM32 USB CONFIGURATION  (Part 13)
    // ============================================================

    generateStm32UsbConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 USB CDC Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'USB Peripheral: ' + (data.usbPeripheral || 'N/A'),
                'USB Class: ' + (data.usbClass || 'N/A'),
                'USB Speed: ' + (data.usbSpeed || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Buffer Configuration', content: [
                'RX Buffer Size: ' + (data.rxBufferSize || 'N/A'),
                'TX Buffer Size: ' + (data.txBufferSize || 'N/A')
            ]},
            { heading: '2. USB Descriptors', content: (data.descriptors || 'Not specified').split('\n') },
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 USB Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32UsbConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 USB CDC Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['USB Peripheral', data.usbPeripheral || ''],
            ['USB Class', data.usbClass || ''],
            ['USB Speed', data.usbSpeed || ''],
            ['RX Buffer Size', data.rxBufferSize || ''],
            ['TX Buffer Size', data.txBufferSize || ''],
            ['USB Descriptors', data.descriptors || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 22 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Config');
        XLSX.writeFile(wb, (filename || 'stm32-usb') + '.xlsx');
    },

    generateStm32UsbConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 USB CDC CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.usbPeripheral || ''), size: 12 },
            { text: 'Class: ' + (data.usbClass || 'N/A') + '  |  Speed: ' + (data.usbSpeed || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── BUFFER CONFIGURATION ──', size: 14, bold: true },
            { text: 'RX: ' + (data.rxBufferSize || 'N/A') + '  |  TX: ' + (data.txBufferSize || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── USB DESCRIPTORS ──', size: 14, bold: true },
            { text: data.descriptors || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 USB Configuration', lines: lines });
    },

    generateStm32UsbConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 USB CDC Configuration',
            subtitle: (data.usbPeripheral || '') + '  |  ' + (data.usbClass || ''),
            perSlide: 2,
            sections: [
                { heading: 'USB Overview', content: 'Peripheral: ' + (data.usbPeripheral || 'N/A') + '\nClass: ' + (data.usbClass || 'N/A') + '\nSpeed: ' + (data.usbSpeed || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Buffer Configuration', content: 'RX: ' + (data.rxBufferSize || 'N/A') + '\nTX: ' + (data.txBufferSize || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'USB Descriptors', content: data.descriptors || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 14. STM32 RTOS DESIGN  (Part 14)
    // ============================================================

    generateStm32RtosDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 FreeRTOS Integration Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'RTOS Version: ' + (data.rtosVersion || 'N/A'),
                'Heap Type: ' + (data.heapType || 'N/A'),
                'Heap Size: ' + (data.heapSize || 'N/A'),
                'Timer Base: ' + (data.timerBase || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Task List', content: (data.taskList || 'Not specified').split('\n') },
            { heading: '2. Queues & IPC', content: (data.queues || 'Not specified').split('\n') },
            { heading: '3. Mutexes & Semaphores', content: (data.mutexes || 'Not specified').split('\n') },
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 RTOS Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32RtosDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 FreeRTOS Integration Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['RTOS Version', data.rtosVersion || ''],
            ['Task List', data.taskList || ''],
            ['Queues & IPC', data.queues || ''],
            ['Mutexes & Semaphores', data.mutexes || ''],
            ['Heap Type', data.heapType || ''],
            ['Heap Size', data.heapSize || ''],
            ['Timer Base', data.timerBase || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'RTOS Design');
        XLSX.writeFile(wb, (filename || 'stm32-rtos') + '.xlsx');
    },

    generateStm32RtosDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 FREERTOS DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.rtosVersion || ''), size: 12 },
            { text: 'Heap: ' + (data.heapType || 'N/A') + ' / ' + (data.heapSize || 'N/A') + '  |  TIM Base: ' + (data.timerBase || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── TASK LIST ──', size: 14, bold: true },
            { text: data.taskList || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── QUEUES & IPC ──', size: 14, bold: true },
            { text: data.queues || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── MUTEXES & SEMAPHORES ──', size: 14, bold: true },
            { text: data.mutexes || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 FreeRTOS Design', lines: lines });
    },

    generateStm32RtosDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 FreeRTOS Integration Design',
            subtitle: (data.rtosVersion || '') + '  |  Heap: ' + (data.heapType || 'N/A'),
            perSlide: 2,
            sections: [
                { heading: 'RTOS Overview', content: 'Version: ' + (data.rtosVersion || 'N/A') + '\nHeap: ' + (data.heapType || 'N/A') + ' / ' + (data.heapSize || 'N/A') + '\nTimer Base: ' + (data.timerBase || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Task List', content: data.taskList || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Queues & IPC', content: data.queues || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Mutexes & Notes', content: (data.mutexes || 'N/A') + '\n' + (data.notes || ''), color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 15. STM32 BOOTLOADER DESIGN  (Part 15)
    // ============================================================

    generateStm32BootloaderDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Bootloader Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Bootloader Size: ' + (data.bootloaderSize || 'N/A'),
                'App Start Address: ' + (data.appStartAddress || 'N/A'),
                'Update Interface: ' + (data.updateInterface || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Image Validation', content: (data.imageValidation || 'Not specified').split('\n') },
            { heading: '2. Flash Layout', content: (data.flashLayout || 'Not specified').split('\n') },
            { heading: '3. Failsafe Strategy', content: (data.failsafeStrategy || 'Not specified').split('\n') },
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Bootloader Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32BootloaderDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 Bootloader Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Target MCU', data.targetMcu || ''],
            ['Bootloader Size', data.bootloaderSize || ''],
            ['App Start Address', data.appStartAddress || ''],
            ['Update Interface', data.updateInterface || ''],
            ['Image Validation', data.imageValidation || ''],
            ['Flash Layout', data.flashLayout || ''],
            ['Failsafe Strategy', data.failsafeStrategy || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Bootloader Design');
        XLSX.writeFile(wb, (filename || 'stm32-bootloader') + '.xlsx');
    },

    generateStm32BootloaderDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 BOOTLOADER DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'BL Size: ' + (data.bootloaderSize || 'N/A') + '  |  App @ ' + (data.appStartAddress || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── UPDATE INTERFACE ──', size: 14, bold: true },
            { text: data.updateInterface || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── IMAGE VALIDATION ──', size: 14, bold: true },
            { text: data.imageValidation || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FLASH LAYOUT ──', size: 14, bold: true },
            { text: data.flashLayout || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FAILSAFE STRATEGY ──', size: 14, bold: true },
            { text: data.failsafeStrategy || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Bootloader Design', lines: lines });
    },

    generateStm32BootloaderDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 Bootloader Design',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  App @ ' + (data.appStartAddress || 'N/A'),
            perSlide: 2,
            sections: [
                { heading: 'Bootloader Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nBL Size: ' + (data.bootloaderSize || 'N/A') + '\nApp Start: ' + (data.appStartAddress || 'N/A') + '\nInterface: ' + (data.updateInterface || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Image Validation', content: data.imageValidation || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Flash Layout', content: data.flashLayout || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Failsafe Strategy & Notes', content: (data.failsafeStrategy || 'N/A') + '\n' + (data.notes || ''), color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 16. STM32 STORAGE CONFIGURATION  (Part 16)
    // ============================================================

    generateStm32StorageConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 External Storage Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Storage Type: ' + (data.storageType || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. SD Card Configuration', content: (data.sdConfig || 'Not specified').split('\n') },
            { heading: '2. QSPI Flash Chip', content: (data.qspiChip || 'Not specified').split('\n') },
            { heading: '3. FATFS Configuration', content: (data.fatfsConfig || 'Not specified').split('\n') },
            { heading: '4. Wear Levelling', content: (data.wearConfig || 'Not specified').split('\n') },
            { heading: '5. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Storage Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32StorageConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 External Storage Configuration'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Storage Type', data.storageType || ''],
            ['SD Card Configuration', data.sdConfig || ''],
            ['QSPI Flash Chip', data.qspiChip || ''],
            ['FATFS Configuration', data.fatfsConfig || ''],
            ['Wear Levelling', data.wearConfig || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 26 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Storage Config');
        XLSX.writeFile(wb, (filename || 'stm32-storage') + '.xlsx');
    },

    generateStm32StorageConfigPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 EXTERNAL STORAGE CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Type: ' + (data.storageType || 'N/A'), size: 12 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── SD CARD CONFIGURATION ──', size: 14, bold: true },
            { text: data.sdConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── QSPI FLASH CHIP ──', size: 14, bold: true },
            { text: data.qspiChip || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FATFS CONFIGURATION ──', size: 14, bold: true },
            { text: data.fatfsConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── WEAR LEVELLING ──', size: 14, bold: true },
            { text: data.wearConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Storage Configuration', lines: lines });
    },

    generateStm32StorageConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 External Storage Configuration',
            subtitle: 'Storage Type: ' + (data.storageType || 'N/A'),
            perSlide: 2,
            sections: [
                { heading: 'Storage Overview', content: 'Type: ' + (data.storageType || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'SD Card & QSPI Flash', content: 'SD: ' + (data.sdConfig || 'N/A') + '\nQSPI: ' + (data.qspiChip || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'FATFS & Wear Levelling', content: 'FATFS: ' + (data.fatfsConfig || 'N/A') + '\nWear: ' + (data.wearConfig || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 17. STM32 ETHERNET DESIGN  (Part 17)
    // ============================================================

    generateStm32EthernetDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Ethernet & TCP/IP Stack Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'PHY Chip: ' + (data.phyChip || 'N/A'),
                'Ethernet Speed: ' + (data.ethSpeed || 'N/A'),
                'MII/RMII: ' + (data.miiRmii || 'N/A'),
                'LwIP Stack: ' + (data.lwipStack || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Network Services', content: (data.networkServices || 'Not specified').split('\n') },
            { heading: '2. LwIP Heap', content: [
                'LwIP Heap: ' + (data.lwipHeap || 'N/A')
            ]},
            { heading: '3. DMA Descriptor Configuration', content: (data.dmaDescriptors || 'Not specified').split('\n') },
            { heading: '4. Notes', content: (data.notes || 'None').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Ethernet Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32EthernetDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 Ethernet & TCP/IP Stack Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['PHY Chip', data.phyChip || ''],
            ['Ethernet Speed', data.ethSpeed || ''],
            ['MII/RMII Interface', data.miiRmii || ''],
            ['LwIP Stack Version', data.lwipStack || ''],
            ['Network Services', data.networkServices || ''],
            ['LwIP Heap', data.lwipHeap || ''],
            ['DMA Descriptors', data.dmaDescriptors || ''],
            ['Notes', data.notes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 26 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Ethernet Design');
        XLSX.writeFile(wb, (filename || 'stm32-ethernet') + '.xlsx');
    },

    generateStm32EthernetDesignPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 ETHERNET & TCP/IP STACK DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  PHY: ' + (data.phyChip || 'N/A'), size: 12 },
            { text: 'Speed: ' + (data.ethSpeed || 'N/A') + '  |  ' + (data.miiRmii || 'N/A') + '  |  LwIP: ' + (data.lwipStack || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NETWORK SERVICES ──', size: 14, bold: true },
            { text: data.networkServices || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── LWIP HEAP ──', size: 14, bold: true },
            { text: 'LwIP Heap: ' + (data.lwipHeap || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── DMA DESCRIPTORS ──', size: 14, bold: true },
            { text: data.dmaDescriptors || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Ethernet Design', lines: lines });
    },

    generateStm32EthernetDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 Ethernet & TCP/IP Stack Design',
            subtitle: 'PHY: ' + (data.phyChip || 'N/A') + '  |  ' + (data.ethSpeed || ''),
            perSlide: 2,
            sections: [
                { heading: 'Ethernet Overview', content: 'PHY: ' + (data.phyChip || 'N/A') + '\nSpeed: ' + (data.ethSpeed || 'N/A') + '\nInterface: ' + (data.miiRmii || 'N/A') + '\nLwIP: ' + (data.lwipStack || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Network Services', content: data.networkServices || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'LwIP Heap & DMA Descriptors', content: 'Heap: ' + (data.lwipHeap || 'N/A') + '\n' + (data.dmaDescriptors || ''), color: DocStyles.colors.blue },
                { heading: 'Notes', content: data.notes || 'None', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 18. STM32 PRODUCTION CHECKLIST  (Part 18)
    // ============================================================

    generateStm32ProductionChecklistWord: async function(filename, data) {
        var sections = [
            { heading: 'STM32 Production Readiness Checklist', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Firmware Version: ' + (data.firmwareVersion || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Watchdog Configuration', content: [
                'Watchdog Config: ' + (data.watchdogConfig || 'N/A'),
                'IWDG Timeout: ' + (data.watchdogTimeout || 'N/A') + ' ms'
            ]},
            { heading: '2. Flash Protection', content: [
                'RDP Level: ' + (data.rdpLevel || 'N/A'),
                'Write-Protected Sectors: ' + (data.writeProtection || 'N/A')
            ]},
            { heading: '3. Code Signing & CI/CD', content: [
                'Code Signing: ' + (data.codeSigning || 'N/A'),
                'CI/CD Platform: ' + (data.cicdPlatform || 'N/A')
            ]},
            { heading: '4. Production Test Plan', content: (data.testPlan || 'Not specified').split('\n') },
            { heading: '5. Release Notes', content: (data.releaseNotes || 'Not specified').split('\n') }
        ];
        return this.generateWord(filename, { title: 'STM32 Production Checklist – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateStm32ProductionChecklistExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['STM32 Production Readiness Checklist'],
            ['Generated by wasilzafar.com STM32 Unleashed Series'],
            [],
            ['Field', 'Value'],
            ['Project Name', data.projectName || ''],
            ['Firmware Version', data.firmwareVersion || ''],
            ['Watchdog Config', data.watchdogConfig || ''],
            ['IWDG Timeout (ms)', data.watchdogTimeout || ''],
            ['RDP Level', data.rdpLevel || ''],
            ['Write-Protected Sectors', data.writeProtection || ''],
            ['Code Signing', data.codeSigning || ''],
            ['CI/CD Platform', data.cicdPlatform || ''],
            ['Test Plan', data.testPlan || ''],
            ['Release Notes', data.releaseNotes || ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 26 }, { wch: 55 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Production Checklist');

        // Second sheet: checklist items
        var checklist = [
            ['Production Readiness Checklist'],
            [],
            ['Category', 'Item', 'Status', 'Notes'],
            ['Watchdog', 'IWDG enabled and timeout configured', data.watchdogConfig !== 'Disabled' ? 'PASS' : 'FAIL', data.watchdogTimeout || ''],
            ['Watchdog', 'Watchdog fed in all code paths', '', ''],
            ['Watchdog', 'WWDG window configured if used', '', ''],
            ['HardFault', 'HardFault handler saves PC/LR/CFSR', '', ''],
            ['HardFault', 'CFSR/BFAR logged via UART or backup register', '', ''],
            ['Flash Protection', 'RDP level set', data.rdpLevel || '', ''],
            ['Flash Protection', 'Bootloader sectors write-protected', data.writeProtection || '', ''],
            ['Code Signing', 'Firmware image validation implemented', data.codeSigning || '', ''],
            ['CI/CD', 'Automated build pipeline active', data.cicdPlatform || '', ''],
            ['CI/CD', 'Binary size check in CI', '', ''],
            ['CI/CD', 'Unit tests pass in CI', '', ''],
            ['Clocks', 'HSE verified on startup', '', ''],
            ['DMA', 'All DMA buffers in DMA-accessible SRAM', '', ''],
            ['NVIC', 'All priorities set explicitly', '', ''],
            ['GPIO', 'Unused GPIO set to analog input', '', ''],
            ['Debug', 'Debug printf removed for production', '', ''],
            ['Version', 'Firmware version string in Flash', data.firmwareVersion || '', ''],
        ];
        var ws2 = XLSX.utils.aoa_to_sheet(checklist);
        ws2['!cols'] = [{ wch: 18 }, { wch: 44 }, { wch: 16 }, { wch: 30 }];
        XLSX.utils.book_append_sheet(wb, ws2, 'Checklist Items');
        XLSX.writeFile(wb, (filename || 'stm32-production') + '.xlsx');
    },

    generateStm32ProductionChecklistPDF: function(filename, data) {
        var lines = [
            { text: 'STM32 PRODUCTION READINESS CHECKLIST', size: 18, bold: true },
            { text: (data.projectName || '') + '  v' + (data.firmwareVersion || 'N/A'), size: 12 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── WATCHDOG CONFIGURATION ──', size: 14, bold: true },
            { text: 'Config: ' + (data.watchdogConfig || 'N/A') + '  |  IWDG Timeout: ' + (data.watchdogTimeout || 'N/A') + ' ms', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FLASH PROTECTION ──', size: 14, bold: true },
            { text: 'RDP Level: ' + (data.rdpLevel || 'N/A'), size: 10 },
            { text: 'Write-Protected Sectors: ' + (data.writeProtection || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── CODE SIGNING & CI/CD ──', size: 14, bold: true },
            { text: 'Signing: ' + (data.codeSigning || 'N/A') + '  |  CI/CD: ' + (data.cicdPlatform || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── PRODUCTION TEST PLAN ──', size: 14, bold: true },
            { text: data.testPlan || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── RELEASE NOTES ──', size: 14, bold: true },
            { text: data.releaseNotes || 'Not specified', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'STM32 Production Readiness Checklist', lines: lines });
    },

    generateStm32ProductionChecklistPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'STM32 Production Readiness Checklist',
            subtitle: (data.projectName || '') + '  v' + (data.firmwareVersion || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Project & Version', content: 'Project: ' + (data.projectName || 'N/A') + '\nFirmware: v' + (data.firmwareVersion || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Watchdog Configuration', content: 'Config: ' + (data.watchdogConfig || 'N/A') + '\nIWDG Timeout: ' + (data.watchdogTimeout || 'N/A') + ' ms', color: DocStyles.colors.teal },
                { heading: 'Flash Protection', content: 'RDP: ' + (data.rdpLevel || 'N/A') + '\nWrite Protection: ' + (data.writeProtection || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Code Signing & CI/CD', content: 'Signing: ' + (data.codeSigning || 'N/A') + '\nCI/CD: ' + (data.cicdPlatform || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Production Test Plan', content: data.testPlan || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Release Notes', content: data.releaseNotes || 'Not specified', color: DocStyles.colors.blue }
            ]
        });
    }

});
