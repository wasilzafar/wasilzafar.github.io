/**
 * doc-generator-usb-dev.js
 * Document generation methods for the USB Development Mastery series.
 * Extends DocGenerator (doc-generator-core.js) via Object.assign.
 * 17 document types × 4 formats = 68 methods.
 *
 * @author wasilzafar.com
 */

Object.assign(DocGenerator, {

    // ============================================================
    // 1. USB SYSTEM ASSESSMENT  (Part 1 — Fundamentals)
    // ============================================================

    generateUsbSystemAssessmentWord: async function(filename, data) {
        var sections = [
            { heading: 'USB System Assessment', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'USB Speed: ' + (data.usbSpeed || 'N/A'),
                'USB Stack: ' + (data.usbStack || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Device Classes', content: (data.deviceClasses || 'Not specified').split('\n') },
            { heading: '2. Transfer Types', content: (data.transferTypes || 'Not specified').split('\n') },
            { heading: '3. Performance', content: [
                'Throughput Target: ' + (data.throughput || 'N/A'),
                'Power Budget: ' + (data.powerBudget || 'N/A')
            ]},
            { heading: '4. Notes', content: ['Review USB specification for selected speed tier and class requirements.'] },
            { heading: '5. Next Steps', content: ['Complete hardware schematic review.', 'Select and configure USB stack library.', 'Define descriptor set for target device class.'] }
        ];
        return this.generateWord(filename, { title: 'USB System Assessment – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbSystemAssessmentExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB System Assessment'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['USB Speed', data.usbSpeed || '', 'FS=12 Mbps, HS=480 Mbps'],
            ['USB Stack', data.usbStack || '', 'e.g. TinyUSB, STM32 USB, libusb'],
            ['Device Classes', data.deviceClasses || '', ''],
            ['Transfer Types', data.transferTypes || '', 'Control/Bulk/Interrupt/Isochronous'],
            ['Throughput Target', data.throughput || '', ''],
            ['Power Budget', data.powerBudget || '', 'USB 2.0 max 500 mA'],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB System Assessment');
        XLSX.writeFile(wb, (filename || 'usb-system-assessment') + '.xlsx');
    },

    generateUsbSystemAssessmentPDF: function(filename, data) {
        var lines = [
            { text: 'USB SYSTEM ASSESSMENT', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Speed: ' + (data.usbSpeed || 'N/A') + '  |  Stack: ' + (data.usbStack || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── DEVICE CLASSES ──', size: 14, bold: true },
            { text: data.deviceClasses || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── TRANSFER TYPES ──', size: 14, bold: true },
            { text: data.transferTypes || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PERFORMANCE ──', size: 14, bold: true },
            { text: 'Throughput: ' + (data.throughput || 'N/A') + '  |  Power: ' + (data.powerBudget || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Complete hardware review, select USB stack, and define descriptor set.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB System Assessment', lines: lines });
    },

    generateUsbSystemAssessmentPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB System Assessment',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Speed: ' + (data.usbSpeed || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Project Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nStack: ' + (data.usbStack || 'N/A') + '\nSpeed: ' + (data.usbSpeed || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Device Classes & Transfers', content: 'Classes: ' + (data.deviceClasses || 'N/A') + '\nTransfers: ' + (data.transferTypes || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Performance Targets', content: 'Throughput: ' + (data.throughput || 'N/A') + '\nPower Budget: ' + (data.powerBudget || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Complete hardware review.\nSelect and configure USB stack.\nDefine descriptor set.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 2. USB HARDWARE CHECKLIST  (Part 2 — Electrical & Hardware)
    // ============================================================

    generateUsbHardwareChecklistWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Hardware Checklist', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'USB Speed: ' + (data.usbSpeed || 'N/A'),
                'Connector Type: ' + (data.connectorType || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. VBUS Protection', content: (data.vbusProtection || 'Not specified').split('\n') },
            { heading: '2. ESD Parts', content: (data.esdParts || 'Not specified').split('\n') },
            { heading: '3. PCB Notes', content: (data.pcbNotes || 'Not specified').split('\n') },
            { heading: '4. Notes', content: ['Verify differential pair impedance (90 Ω), keep traces short, add decoupling capacitors near USB PHY.'] },
            { heading: '5. Next Steps', content: ['Submit for PCB review.', 'Order ESD and protection components.', 'Verify connector footprint dimensions.'] }
        ];
        return this.generateWord(filename, { title: 'USB Hardware Checklist – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbHardwareChecklistExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Hardware Checklist'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['USB Speed', data.usbSpeed || '', ''],
            ['Connector Type', data.connectorType || '', 'USB-A, USB-C, Micro-B, etc.'],
            ['VBUS Protection', data.vbusProtection || '', 'Polyfuse / TVS diode'],
            ['ESD Parts', data.esdParts || '', 'e.g. USBLC6-2SC6'],
            ['PCB Notes', data.pcbNotes || '', '90 Ω diff pairs'],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB HW Checklist');
        XLSX.writeFile(wb, (filename || 'usb-hardware-checklist') + '.xlsx');
    },

    generateUsbHardwareChecklistPDF: function(filename, data) {
        var lines = [
            { text: 'USB HARDWARE CHECKLIST', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Speed: ' + (data.usbSpeed || 'N/A') + '  |  Connector: ' + (data.connectorType || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── VBUS PROTECTION ──', size: 14, bold: true },
            { text: data.vbusProtection || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── ESD PARTS ──', size: 14, bold: true },
            { text: data.esdParts || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PCB NOTES ──', size: 14, bold: true },
            { text: data.pcbNotes || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Submit PCB for review. Order protection components. Verify connector footprint.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Hardware Checklist', lines: lines });
    },

    generateUsbHardwareChecklistPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Hardware Checklist',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Connector: ' + (data.connectorType || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Hardware Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nSpeed: ' + (data.usbSpeed || 'N/A') + '\nConnector: ' + (data.connectorType || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Protection & ESD', content: 'VBUS: ' + (data.vbusProtection || 'N/A') + '\nESD Parts: ' + (data.esdParts || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'PCB Notes', content: data.pcbNotes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Submit PCB for review.\nOrder protection components.\nVerify connector footprint.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 3. USB ENUMERATION DESIGN  (Part 3 — Protocol & Enumeration)
    // ============================================================

    generateUsbEnumerationDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Enumeration Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Vendor ID (VID): ' + (data.vid || 'N/A'),
                'Product ID (PID): ' + (data.pid || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. String Descriptors', content: [
                'Manufacturer: ' + (data.manufacturerString || 'N/A'),
                'Product: ' + (data.productString || 'N/A'),
                'Serial: ' + (data.serialString || 'N/A')
            ]},
            { heading: '2. Device Classes', content: (data.deviceClasses || 'Not specified').split('\n') },
            { heading: '3. Notes', content: ['Obtain a valid VID from USB-IF or use a licensed sub-VID. Ensure PID is unique per product variant.'] },
            { heading: '4. Next Steps', content: ['Test enumeration with USBView or lsusb.', 'Verify descriptor hierarchy with USB descriptor validator.', 'Test on Windows, Linux, and macOS hosts.'] }
        ];
        return this.generateWord(filename, { title: 'USB Enumeration Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbEnumerationDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Enumeration Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Vendor ID (VID)', data.vid || '', 'Obtain from USB-IF'],
            ['Product ID (PID)', data.pid || '', 'Unique per product'],
            ['Manufacturer String', data.manufacturerString || '', ''],
            ['Product String', data.productString || '', ''],
            ['Serial String', data.serialString || '', 'Can be dynamic'],
            ['Device Classes', data.deviceClasses || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 26 }, { wch: 38 }, { wch: 34 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Enumeration');
        XLSX.writeFile(wb, (filename || 'usb-enumeration-design') + '.xlsx');
    },

    generateUsbEnumerationDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB ENUMERATION DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'VID: ' + (data.vid || 'N/A') + '  |  PID: ' + (data.pid || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── STRING DESCRIPTORS ──', size: 14, bold: true },
            { text: 'Manufacturer: ' + (data.manufacturerString || 'N/A'), size: 10 },
            { text: 'Product: ' + (data.productString || 'N/A'), size: 10 },
            { text: 'Serial: ' + (data.serialString || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── DEVICE CLASSES ──', size: 14, bold: true },
            { text: data.deviceClasses || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Test enumeration with USBView or lsusb. Validate descriptor hierarchy.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Enumeration Design', lines: lines });
    },

    generateUsbEnumerationDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Enumeration Design',
            subtitle: 'VID: ' + (data.vid || 'N/A') + '  PID: ' + (data.pid || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Identifiers', content: 'VID: ' + (data.vid || 'N/A') + '\nPID: ' + (data.pid || 'N/A') + '\nMCU: ' + (data.targetMcu || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'String Descriptors', content: 'Manufacturer: ' + (data.manufacturerString || 'N/A') + '\nProduct: ' + (data.productString || 'N/A') + '\nSerial: ' + (data.serialString || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Device Classes', content: data.deviceClasses || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Test with USBView / lsusb.\nValidate descriptor hierarchy.\nTest on all major OS.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 4. USB CLASS SELECTION  (Part 4 — Device Classes)
    // ============================================================

    generateUsbClassSelectionWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Class Selection', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Primary Class: ' + (data.primaryClass || 'N/A'),
                'Secondary Class: ' + (data.secondaryClass || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Rationale', content: (data.rationale || 'Not specified').split('\n') },
            { heading: '2. Descriptors', content: (data.descriptors || 'Not specified').split('\n') },
            { heading: '3. Notes', content: ['Choose class codes that align with OS driver availability. Custom/vendor-specific classes require a host-side driver.'] },
            { heading: '4. Next Steps', content: ['Implement class descriptors in firmware.', 'Test OS enumeration for each class.', 'Document any custom class protocol.'] }
        ];
        return this.generateWord(filename, { title: 'USB Class Selection – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbClassSelectionExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Class Selection'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Primary Class', data.primaryClass || '', 'e.g. CDC, HID, MSC, Audio'],
            ['Secondary Class', data.secondaryClass || '', 'Optional composite class'],
            ['Rationale', data.rationale || '', ''],
            ['Descriptors', data.descriptors || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 34 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Class Selection');
        XLSX.writeFile(wb, (filename || 'usb-class-selection') + '.xlsx');
    },

    generateUsbClassSelectionPDF: function(filename, data) {
        var lines = [
            { text: 'USB CLASS SELECTION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Primary: ' + (data.primaryClass || 'N/A') + '  |  Secondary: ' + (data.secondaryClass || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── RATIONALE ──', size: 14, bold: true },
            { text: data.rationale || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── DESCRIPTORS ──', size: 14, bold: true },
            { text: data.descriptors || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Implement class descriptors in firmware. Test OS enumeration.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Class Selection', lines: lines });
    },

    generateUsbClassSelectionPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Class Selection',
            subtitle: 'Primary: ' + (data.primaryClass || 'N/A') + '  |  Secondary: ' + (data.secondaryClass || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Class Overview', content: 'Primary: ' + (data.primaryClass || 'N/A') + '\nSecondary: ' + (data.secondaryClass || 'N/A') + '\nMCU: ' + (data.targetMcu || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Rationale', content: data.rationale || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Descriptors', content: data.descriptors || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Implement class descriptors.\nTest OS enumeration.\nDocument custom protocol.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 5. TINYUSB PORTING PLAN  (Part 5 — TinyUSB Deep Dive)
    // ============================================================

    generateTinyUsbPortingPlanWord: async function(filename, data) {
        var sections = [
            { heading: 'TinyUSB Porting Plan', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'OS Type: ' + (data.osType || 'N/A'),
                'USB Classes: ' + (data.usbClasses || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Memory Configuration', content: [
                'Memory Section: ' + (data.memSection || 'N/A')
            ]},
            { heading: '2. Debug Level', content: [
                'Debug Level: ' + (data.debugLevel || 'N/A')
            ]},
            { heading: '3. Notes', content: (data.notes || 'Not specified').split('\n') },
            { heading: '4. Next Steps', content: ['Create tusb_config.h with class and OS settings.', 'Implement board BSP callbacks.', 'Verify usb_task loop under selected RTOS or bare-metal.'] }
        ];
        return this.generateWord(filename, { title: 'TinyUSB Porting Plan – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateTinyUsbPortingPlanExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['TinyUSB Porting Plan'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['OS Type', data.osType || '', 'FreeRTOS, bare-metal, etc.'],
            ['USB Classes', data.usbClasses || '', ''],
            ['Memory Section', data.memSection || '', 'e.g. .usb_ram, DTCM'],
            ['Debug Level', data.debugLevel || '', '0=off, 1=error, 2=warning'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 34 }];
        XLSX.utils.book_append_sheet(wb, ws, 'TinyUSB Porting');
        XLSX.writeFile(wb, (filename || 'tinyusb-porting-plan') + '.xlsx');
    },

    generateTinyUsbPortingPlanPDF: function(filename, data) {
        var lines = [
            { text: 'TINYUSB PORTING PLAN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'OS: ' + (data.osType || 'N/A') + '  |  Classes: ' + (data.usbClasses || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── MEMORY CONFIGURATION ──', size: 14, bold: true },
            { text: 'Memory Section: ' + (data.memSection || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── DEBUG LEVEL ──', size: 14, bold: true },
            { text: 'Debug Level: ' + (data.debugLevel || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Create tusb_config.h, implement BSP callbacks, verify usb_task loop.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'TinyUSB Porting Plan', lines: lines });
    },

    generateTinyUsbPortingPlanPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'TinyUSB Porting Plan',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  OS: ' + (data.osType || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Porting Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nOS: ' + (data.osType || 'N/A') + '\nClasses: ' + (data.usbClasses || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Memory & Debug', content: 'Memory Section: ' + (data.memSection || 'N/A') + '\nDebug Level: ' + (data.debugLevel || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Create tusb_config.h.\nImplement BSP callbacks.\nVerify usb_task loop.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 6. USB CDC DESIGN  (Part 6 — CDC Virtual COM Port)
    // ============================================================

    generateUsbCdcDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB CDC Virtual COM Port Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Baud Rate: ' + (data.baudRate || 'N/A'),
                'Bridge UART: ' + (data.bridgeUart || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Buffer Configuration', content: [
                'RX Buffer Size: ' + (data.rxBufSize || 'N/A'),
                'TX Buffer Size: ' + (data.txBufSize || 'N/A'),
                'Flow Control: ' + (data.flowControl || 'N/A')
            ]},
            { heading: '2. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '3. Next Steps', content: ['Implement CDC callbacks in firmware.', 'Test with a serial terminal (e.g. PuTTY, minicom).', 'Validate data throughput at target baud rate.'] }
        ];
        return this.generateWord(filename, { title: 'USB CDC Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbCdcDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB CDC Virtual COM Port Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Baud Rate', data.baudRate || '', 'Symbolic; USB CDC ignores real baud'],
            ['Bridge UART', data.bridgeUart || '', 'UART instance forwarded over USB'],
            ['RX Buffer Size', data.rxBufSize || '', 'Bytes'],
            ['TX Buffer Size', data.txBufSize || '', 'Bytes'],
            ['Flow Control', data.flowControl || '', 'None / RTS-CTS / XON-XOFF'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB CDC Design');
        XLSX.writeFile(wb, (filename || 'usb-cdc-design') + '.xlsx');
    },

    generateUsbCdcDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB CDC VIRTUAL COM PORT DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Baud: ' + (data.baudRate || 'N/A') + '  |  Bridge UART: ' + (data.bridgeUart || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── BUFFER CONFIGURATION ──', size: 14, bold: true },
            { text: 'RX: ' + (data.rxBufSize || 'N/A') + '  |  TX: ' + (data.txBufSize || 'N/A') + '  |  Flow: ' + (data.flowControl || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Implement CDC callbacks. Test with serial terminal. Validate throughput.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB CDC Design', lines: lines });
    },

    generateUsbCdcDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB CDC Virtual COM Port Design',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Baud: ' + (data.baudRate || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'CDC Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nBaud: ' + (data.baudRate || 'N/A') + '\nBridge UART: ' + (data.bridgeUart || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Buffer Config', content: 'RX: ' + (data.rxBufSize || 'N/A') + '\nTX: ' + (data.txBufSize || 'N/A') + '\nFlow Control: ' + (data.flowControl || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Implement CDC callbacks.\nTest with serial terminal.\nValidate data throughput.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 7. USB HID DESIGN  (Part 7 — HID Devices)
    // ============================================================

    generateUsbHidDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB HID Device Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'HID Type: ' + (data.hidType || 'N/A'),
                'Poll Interval: ' + (data.pollInterval || 'N/A') + ' ms',
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Report Configuration', content: [
                'Report IDs: ' + (data.reportIds || 'N/A')
            ]},
            { heading: '2. Report Descriptor', content: (data.reportDescriptor || 'Not specified').split('\n') },
            { heading: '3. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '4. Next Steps', content: ['Validate HID report descriptor with HID Descriptor Tool.', 'Test input reports on all target OS.', 'Verify poll interval meets latency requirements.'] }
        ];
        return this.generateWord(filename, { title: 'USB HID Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbHidDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB HID Device Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['HID Type', data.hidType || '', 'Keyboard / Mouse / Gamepad / Custom'],
            ['Report IDs', data.reportIds || '', '0 if single report, else per-report ID'],
            ['Poll Interval', data.pollInterval || '', 'ms; lower = more responsive'],
            ['Report Descriptor', data.reportDescriptor || '', ''],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB HID Design');
        XLSX.writeFile(wb, (filename || 'usb-hid-design') + '.xlsx');
    },

    generateUsbHidDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB HID DEVICE DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'HID Type: ' + (data.hidType || 'N/A') + '  |  Poll: ' + (data.pollInterval || 'N/A') + ' ms', size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── REPORT CONFIGURATION ──', size: 14, bold: true },
            { text: 'Report IDs: ' + (data.reportIds || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── REPORT DESCRIPTOR ──', size: 14, bold: true },
            { text: data.reportDescriptor || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Validate report descriptor. Test on target OS. Verify poll interval.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB HID Design', lines: lines });
    },

    generateUsbHidDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB HID Device Design',
            subtitle: 'Type: ' + (data.hidType || 'N/A') + '  |  Poll: ' + (data.pollInterval || 'N/A') + ' ms  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'HID Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nType: ' + (data.hidType || 'N/A') + '\nPoll: ' + (data.pollInterval || 'N/A') + ' ms', color: DocStyles.colors.navy },
                { heading: 'Report Config', content: 'Report IDs: ' + (data.reportIds || 'N/A') + '\n\nDescriptor:\n' + (data.reportDescriptor || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Validate report descriptor.\nTest on target OS.\nVerify poll interval.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 8. USB MSC DESIGN  (Part 8 — Mass Storage)
    // ============================================================

    generateUsbMscDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Mass Storage Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Storage Backend: ' + (data.storageBackend || 'N/A'),
                'LUN Count: ' + (data.lunCount || 'N/A'),
                'Block Size: ' + (data.blockSize || 'N/A'),
                'Write Protect: ' + (data.writeProtect || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Implement SCSI callback functions.', 'Test read/write throughput with host file system.', 'Validate safe removal on Windows and Linux.'] }
        ];
        return this.generateWord(filename, { title: 'USB MSC Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbMscDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Mass Storage Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Storage Backend', data.storageBackend || '', 'SDMMC, QSPI Flash, Internal Flash'],
            ['LUN Count', data.lunCount || '', 'Logical Unit Numbers'],
            ['Block Size', data.blockSize || '', 'Typically 512 bytes'],
            ['Write Protect', data.writeProtect || '', 'Yes / No'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB MSC Design');
        XLSX.writeFile(wb, (filename || 'usb-msc-design') + '.xlsx');
    },

    generateUsbMscDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB MASS STORAGE DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Backend: ' + (data.storageBackend || 'N/A') + '  |  LUNs: ' + (data.lunCount || 'N/A') + '  |  Block: ' + (data.blockSize || 'N/A'), size: 10 },
            { text: 'Write Protect: ' + (data.writeProtect || 'N/A') + '  |  Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Implement SCSI callbacks. Test throughput. Validate safe removal.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Mass Storage Design', lines: lines });
    },

    generateUsbMscDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Mass Storage Design',
            subtitle: 'Backend: ' + (data.storageBackend || 'N/A') + '  |  Block: ' + (data.blockSize || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'MSC Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nBackend: ' + (data.storageBackend || 'N/A') + '\nLUNs: ' + (data.lunCount || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Storage Configuration', content: 'Block Size: ' + (data.blockSize || 'N/A') + '\nWrite Protect: ' + (data.writeProtect || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Implement SCSI callbacks.\nTest read/write throughput.\nValidate safe removal.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 9. USB COMPOSITE DESIGN  (Part 9 — Composite Devices)
    // ============================================================

    generateUsbCompositeDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Composite Device Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Class 1: ' + (data.class1 || 'N/A'),
                'Class 2: ' + (data.class2 || 'N/A'),
                'Class 3: ' + (data.class3 || 'N/A'),
                'Total Interfaces: ' + (data.totalInterfaces || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Define IAD descriptors for each function.', 'Allocate interface numbers without overlap.', 'Test composite enumeration on all target OS.'] }
        ];
        return this.generateWord(filename, { title: 'USB Composite Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbCompositeDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Composite Device Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Class 1', data.class1 || '', ''],
            ['Class 2', data.class2 || '', ''],
            ['Class 3', data.class3 || '', ''],
            ['Total Interfaces', data.totalInterfaces || '', 'Sum of all class interfaces'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Composite');
        XLSX.writeFile(wb, (filename || 'usb-composite-design') + '.xlsx');
    },

    generateUsbCompositeDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB COMPOSITE DEVICE DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Classes: ' + [data.class1, data.class2, data.class3].filter(Boolean).join(' + ') + '  |  Interfaces: ' + (data.totalInterfaces || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── CLASS BREAKDOWN ──', size: 14, bold: true },
            { text: 'Class 1: ' + (data.class1 || 'N/A'), size: 10 },
            { text: 'Class 2: ' + (data.class2 || 'N/A'), size: 10 },
            { text: 'Class 3: ' + (data.class3 || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Define IAD descriptors. Allocate interfaces. Test composite enumeration.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Composite Design', lines: lines });
    },

    generateUsbCompositeDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Composite Device Design',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Interfaces: ' + (data.totalInterfaces || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Composite Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nTotal Interfaces: ' + (data.totalInterfaces || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Class Assignments', content: 'Class 1: ' + (data.class1 || 'N/A') + '\nClass 2: ' + (data.class2 || 'N/A') + '\nClass 3: ' + (data.class3 || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Define IAD descriptors.\nAllocate interface numbers.\nTest composite enumeration.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 10. USB DEBUG PLAN  (Part 10 — Debugging)
    // ============================================================

    generateUsbDebugPlanWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Debug Plan', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Debug Tool: ' + (data.debugTool || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Issue Description', content: (data.issueDescription || 'Not specified').split('\n') },
            { heading: '2. Capture Path', content: (data.capturePath || 'Not specified').split('\n') },
            { heading: '3. Findings', content: (data.findings || 'Not specified').split('\n') },
            { heading: '4. Notes', content: ['Use a USB protocol analyzer (e.g. Ellisys, Beagle) for deep enumeration and transfer analysis.'] },
            { heading: '5. Next Steps', content: ['Replicate issue with minimal test firmware.', 'Capture USB traffic and annotate key frames.', 'Cross-reference against USB 2.0 specification.'] }
        ];
        return this.generateWord(filename, { title: 'USB Debug Plan – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbDebugPlanExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Debug Plan'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Debug Tool', data.debugTool || '', 'e.g. Wireshark + usbmon, Ellisys, Beagle'],
            ['Issue Description', data.issueDescription || '', ''],
            ['Capture Path', data.capturePath || '', 'File path or URL to capture'],
            ['Findings', data.findings || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 44 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Debug Plan');
        XLSX.writeFile(wb, (filename || 'usb-debug-plan') + '.xlsx');
    },

    generateUsbDebugPlanPDF: function(filename, data) {
        var lines = [
            { text: 'USB DEBUG PLAN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Tool: ' + (data.debugTool || 'N/A') + '  |  Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── ISSUE DESCRIPTION ──', size: 14, bold: true },
            { text: data.issueDescription || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── CAPTURE PATH ──', size: 14, bold: true },
            { text: data.capturePath || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FINDINGS ──', size: 14, bold: true },
            { text: data.findings || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Replicate with minimal firmware. Capture and annotate traffic. Check spec.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Debug Plan', lines: lines });
    },

    generateUsbDebugPlanPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Debug Plan',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Tool: ' + (data.debugTool || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Debug Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nTool: ' + (data.debugTool || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Issue Description', content: data.issueDescription || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Capture & Findings', content: 'Path: ' + (data.capturePath || 'N/A') + '\n\nFindings: ' + (data.findings || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Replicate with minimal firmware.\nCapture and annotate traffic.\nCross-reference USB 2.0 spec.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 11. USB RTOS DESIGN  (Part 11 — RTOS + USB)
    // ============================================================

    generateUsbRtosDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB RTOS Integration Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'RTOS Version: ' + (data.rtosVersion || 'N/A'),
                'USB Task Priority: ' + (data.usbTaskPriority || 'N/A'),
                'USB Task Stack: ' + (data.usbTaskStack || 'N/A') + ' words',
                'Queue Size: ' + (data.queueSize || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Create usb_task with defined priority and stack.', 'Use thread-safe queues for USB data exchange.', 'Verify no priority inversion with USB callbacks.'] }
        ];
        return this.generateWord(filename, { title: 'USB RTOS Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbRtosDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB RTOS Integration Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['RTOS Version', data.rtosVersion || '', 'e.g. FreeRTOS v10.5'],
            ['USB Task Priority', data.usbTaskPriority || '', 'Higher than application tasks'],
            ['USB Task Stack', data.usbTaskStack || '', 'Words (4 bytes each)'],
            ['Queue Size', data.queueSize || '', 'Messages in queue'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB RTOS Design');
        XLSX.writeFile(wb, (filename || 'usb-rtos-design') + '.xlsx');
    },

    generateUsbRtosDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB RTOS INTEGRATION DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'RTOS: ' + (data.rtosVersion || 'N/A') + '  |  Priority: ' + (data.usbTaskPriority || 'N/A'), size: 10 },
            { text: 'Stack: ' + (data.usbTaskStack || 'N/A') + ' words  |  Queue: ' + (data.queueSize || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Create usb_task with defined priority. Use thread-safe queues. Verify no priority inversion.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB RTOS Integration Design', lines: lines });
    },

    generateUsbRtosDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB RTOS Integration Design',
            subtitle: 'RTOS: ' + (data.rtosVersion || 'N/A') + '  |  MCU: ' + (data.targetMcu || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'RTOS Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nRTOS: ' + (data.rtosVersion || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Task Configuration', content: 'Priority: ' + (data.usbTaskPriority || 'N/A') + '\nStack: ' + (data.usbTaskStack || 'N/A') + ' words\nQueue: ' + (data.queueSize || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Create usb_task with priority.\nUse thread-safe queues.\nVerify no priority inversion.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 12. USB ADVANCED CONFIG  (Part 12 — Advanced Topics)
    // ============================================================

    generateUsbAdvancedConfigWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Advanced Configuration', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Advanced Feature: ' + (data.advancedFeature || 'N/A'),
                'Isochronous Transfer: ' + (data.isoTransfer || 'N/A'),
                'Remote Wakeup: ' + (data.remoteWakeup || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Implement advanced feature in firmware.', 'Test edge cases (bus reset, suspend/resume).', 'Profile CPU load for isochronous data paths.'] }
        ];
        return this.generateWord(filename, { title: 'USB Advanced Config – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbAdvancedConfigExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Advanced Configuration'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Advanced Feature', data.advancedFeature || '', 'e.g. OTG, Link Power Mgmt, LPM'],
            ['Isochronous Transfer', data.isoTransfer || '', 'Yes / No'],
            ['Remote Wakeup', data.remoteWakeup || '', 'Yes / No'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 26 }, { wch: 40 }, { wch: 34 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Advanced Config');
        XLSX.writeFile(wb, (filename || 'usb-advanced-config') + '.xlsx');
    },

    generateUsbAdvancedConfigPDF: function(filename, data) {
        var lines = [
            { text: 'USB ADVANCED CONFIGURATION', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Feature: ' + (data.advancedFeature || 'N/A') + '  |  ISO: ' + (data.isoTransfer || 'N/A') + '  |  Wakeup: ' + (data.remoteWakeup || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Implement advanced feature. Test edge cases. Profile CPU for isochronous paths.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Advanced Configuration', lines: lines });
    },

    generateUsbAdvancedConfigPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Advanced Configuration',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Feature: ' + (data.advancedFeature || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Advanced Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nFeature: ' + (data.advancedFeature || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Key Options', content: 'Isochronous: ' + (data.isoTransfer || 'N/A') + '\nRemote Wakeup: ' + (data.remoteWakeup || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Implement advanced feature.\nTest edge cases.\nProfile CPU load.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 13. USB PERFORMANCE PLAN  (Part 13 — Performance)
    // ============================================================

    generateUsbPerformancePlanWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Performance Plan', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'USB Speed: ' + (data.usbSpeed || 'N/A'),
                'USB Class: ' + (data.usbClass || 'N/A'),
                'Target Mbps: ' + (data.targetMbps || 'N/A'),
                'DMA Enabled: ' + (data.dmaEnabled || 'N/A'),
                'Double Buffer: ' + (data.doubleBuffer || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Enable DMA for USB data paths.', 'Profile with USB analyzer at target bandwidth.', 'Tune buffer sizes and double-buffering configuration.'] }
        ];
        return this.generateWord(filename, { title: 'USB Performance Plan – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbPerformancePlanExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Performance Plan'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['USB Speed', data.usbSpeed || '', 'FS / HS'],
            ['USB Class', data.usbClass || '', ''],
            ['Target Throughput', data.targetMbps || '', 'Mbps'],
            ['DMA Enabled', data.dmaEnabled || '', 'Yes / No'],
            ['Double Buffer', data.doubleBuffer || '', 'Yes / No'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 34 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Performance');
        XLSX.writeFile(wb, (filename || 'usb-performance-plan') + '.xlsx');
    },

    generateUsbPerformancePlanPDF: function(filename, data) {
        var lines = [
            { text: 'USB PERFORMANCE PLAN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Speed: ' + (data.usbSpeed || 'N/A') + '  |  Class: ' + (data.usbClass || 'N/A') + '  |  Target: ' + (data.targetMbps || 'N/A') + ' Mbps', size: 10 },
            { text: 'DMA: ' + (data.dmaEnabled || 'N/A') + '  |  Double Buffer: ' + (data.doubleBuffer || 'N/A') + '  |  Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Enable DMA. Profile with USB analyzer. Tune buffers and double-buffering.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Performance Plan', lines: lines });
    },

    generateUsbPerformancePlanPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Performance Plan',
            subtitle: 'Speed: ' + (data.usbSpeed || 'N/A') + '  |  Target: ' + (data.targetMbps || 'N/A') + ' Mbps  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Performance Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nSpeed: ' + (data.usbSpeed || 'N/A') + '\nClass: ' + (data.usbClass || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Optimization Config', content: 'Target: ' + (data.targetMbps || 'N/A') + ' Mbps\nDMA: ' + (data.dmaEnabled || 'N/A') + '\nDouble Buffer: ' + (data.doubleBuffer || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Enable DMA for USB paths.\nProfile at target bandwidth.\nTune buffer sizes.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 14. USB CUSTOM DRIVER DESIGN  (Part 14 — Custom Class Drivers)
    // ============================================================

    generateUsbCustomDriverDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Custom Class Driver Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Vendor ID (VID): ' + (data.vid || 'N/A'),
                'Product ID (PID): ' + (data.pid || 'N/A'),
                'Bulk IN: ' + (data.hasBulkIn || 'N/A'),
                'Bulk OUT: ' + (data.hasBulkOut || 'N/A'),
                'Interrupt IN: ' + (data.hasInterruptIn || 'N/A'),
                'WinUSB Enabled: ' + (data.winUsbEnabled || 'N/A'),
                'Protocol: ' + (data.protocol || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Protocol Details', content: (data.protocol || 'Not specified').split('\n') },
            { heading: '2. Notes', content: ['WinUSB allows driver-less host access on Windows 8+. Include MS OS 2.0 descriptors for automatic binding.'] },
            { heading: '3. Next Steps', content: ['Write host-side driver or use WinUSB/libusb.', 'Define binary device object store (BOS) descriptor.', 'Test custom protocol end-to-end.'] }
        ];
        return this.generateWord(filename, { title: 'USB Custom Driver Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbCustomDriverDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Custom Class Driver Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Vendor ID (VID)', data.vid || '', ''],
            ['Product ID (PID)', data.pid || '', ''],
            ['Has Bulk IN', data.hasBulkIn || '', 'Yes / No'],
            ['Has Bulk OUT', data.hasBulkOut || '', 'Yes / No'],
            ['Has Interrupt IN', data.hasInterruptIn || '', 'Yes / No'],
            ['WinUSB Enabled', data.winUsbEnabled || '', 'Yes / No'],
            ['Protocol', data.protocol || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 34 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Custom Driver');
        XLSX.writeFile(wb, (filename || 'usb-custom-driver-design') + '.xlsx');
    },

    generateUsbCustomDriverDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB CUSTOM CLASS DRIVER DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'VID: ' + (data.vid || 'N/A') + '  |  PID: ' + (data.pid || 'N/A') + '  |  WinUSB: ' + (data.winUsbEnabled || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── ENDPOINT CONFIGURATION ──', size: 14, bold: true },
            { text: 'Bulk IN: ' + (data.hasBulkIn || 'N/A') + '  |  Bulk OUT: ' + (data.hasBulkOut || 'N/A') + '  |  Interrupt IN: ' + (data.hasInterruptIn || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── PROTOCOL ──', size: 14, bold: true },
            { text: data.protocol || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Write host driver or use WinUSB/libusb. Define BOS descriptor. Test protocol end-to-end.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Custom Driver Design', lines: lines });
    },

    generateUsbCustomDriverDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Custom Class Driver Design',
            subtitle: 'VID: ' + (data.vid || 'N/A') + '  PID: ' + (data.pid || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Driver Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nVID: ' + (data.vid || 'N/A') + '  PID: ' + (data.pid || 'N/A') + '\nWinUSB: ' + (data.winUsbEnabled || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Endpoint Config', content: 'Bulk IN: ' + (data.hasBulkIn || 'N/A') + '\nBulk OUT: ' + (data.hasBulkOut || 'N/A') + '\nInterrupt IN: ' + (data.hasInterruptIn || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Protocol', content: data.protocol || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Write host driver or use WinUSB.\nDefine BOS descriptor.\nTest custom protocol end-to-end.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 15. BARE-METAL USB DESIGN  (Part 15 — Bare-Metal USB)
    // ============================================================

    generateBareMetalUsbDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'Bare-Metal USB Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'USB Peripheral: ' + (data.peripheral || 'N/A'),
                'Endpoint Count: ' + (data.endpointCount || 'N/A'),
                'PMA Size: ' + (data.pmaSize || 'N/A') + ' bytes',
                'USB Classes: ' + (data.usbClasses || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Map endpoints in PMA/FIFO.', 'Implement USB interrupt handler state machine.', 'Handle RESET, SETUP, and STATUS stages manually.'] }
        ];
        return this.generateWord(filename, { title: 'Bare-Metal USB Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateBareMetalUsbDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Bare-Metal USB Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['USB Peripheral', data.peripheral || '', 'e.g. USB_FS, USB_OTG_FS'],
            ['Endpoint Count', data.endpointCount || '', 'Including EP0'],
            ['PMA Size', data.pmaSize || '', 'Packet Memory Area in bytes'],
            ['USB Classes', data.usbClasses || '', ''],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Bare-Metal USB');
        XLSX.writeFile(wb, (filename || 'bare-metal-usb-design') + '.xlsx');
    },

    generateBareMetalUsbDesignPDF: function(filename, data) {
        var lines = [
            { text: 'BARE-METAL USB DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Peripheral: ' + (data.peripheral || 'N/A') + '  |  EPs: ' + (data.endpointCount || 'N/A') + '  |  PMA: ' + (data.pmaSize || 'N/A') + ' B', size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── USB CLASSES ──', size: 14, bold: true },
            { text: data.usbClasses || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Map endpoints in PMA. Implement interrupt handler state machine. Handle RESET/SETUP/STATUS.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Bare-Metal USB Design', lines: lines });
    },

    generateBareMetalUsbDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Bare-Metal USB Design',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  Peripheral: ' + (data.peripheral || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Peripheral Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nPeripheral: ' + (data.peripheral || 'N/A') + '\nEndpoints: ' + (data.endpointCount || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Memory Config', content: 'PMA Size: ' + (data.pmaSize || 'N/A') + ' bytes\nClasses: ' + (data.usbClasses || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Map endpoints in PMA.\nImplement ISR state machine.\nHandle RESET/SETUP/STATUS.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 16. USB SECURITY AUDIT  (Part 16 — Security)
    // ============================================================

    generateUsbSecurityAuditWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Security Audit', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'RDP Level: ' + (data.rdpLevel || 'N/A'),
                'Signed DFU: ' + (data.signedDfu || 'N/A'),
                'VBUS Protection: ' + (data.vbusProtection || 'N/A'),
                'Host Validation: ' + (data.hostValidation || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Enable RDP level 2 for production builds.', 'Implement signed DFU with secure boot chain.', 'Validate USB host before exposing sensitive data paths.'] }
        ];
        return this.generateWord(filename, { title: 'USB Security Audit – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbSecurityAuditExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Security Audit'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['RDP Level', data.rdpLevel || '', '0=None, 1=Read protect, 2=Full'],
            ['Signed DFU', data.signedDfu || '', 'Yes / No'],
            ['VBUS Protection', data.vbusProtection || '', 'TVS / polyfuse'],
            ['Host Validation', data.hostValidation || '', 'Challenge-response / certificate'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB Security Audit');
        XLSX.writeFile(wb, (filename || 'usb-security-audit') + '.xlsx');
    },

    generateUsbSecurityAuditPDF: function(filename, data) {
        var lines = [
            { text: 'USB SECURITY AUDIT', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'RDP: ' + (data.rdpLevel || 'N/A') + '  |  Signed DFU: ' + (data.signedDfu || 'N/A') + '  |  VBUS: ' + (data.vbusProtection || 'N/A'), size: 10 },
            { text: 'Host Validation: ' + (data.hostValidation || 'N/A') + '  |  Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Enable RDP level 2. Implement signed DFU. Validate USB host before sensitive operations.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Security Audit', lines: lines });
    },

    generateUsbSecurityAuditPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Security Audit',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  RDP: ' + (data.rdpLevel || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Security Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nRDP Level: ' + (data.rdpLevel || 'N/A') + '\nSigned DFU: ' + (data.signedDfu || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Protection Measures', content: 'VBUS Protection: ' + (data.vbusProtection || 'N/A') + '\nHost Validation: ' + (data.hostValidation || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Key Notes', content: data.notes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Enable RDP level 2.\nImplement signed DFU.\nValidate USB host before sensitive data.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 17. USB HARDWARE DESIGN  (Part 17 — Hardware Design)
    // ============================================================

    generateUsbHardwareDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'USB Hardware Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Target MCU: ' + (data.targetMcu || 'N/A'),
                'Connector Type: ' + (data.connectorType || 'N/A'),
                'USB Speed: ' + (data.usbSpeed || 'N/A'),
                'PCB Layers: ' + (data.pcbLayers || 'N/A'),
                'ESD Protection: ' + (data.esdProtection || 'N/A'),
                'Polyfuse: ' + (data.polyfuse || 'N/A'),
                'Clock Source: ' + (data.clockSource || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Notes', content: (data.notes || 'None').split('\n') },
            { heading: '2. Next Steps', content: ['Complete PCB layout with 90 Ω differential pair routing.', 'Add TVS diodes and polyfuse to final BOM.', 'Perform signal integrity simulation if HS USB is used.'] }
        ];
        return this.generateWord(filename, { title: 'USB Hardware Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateUsbHardwareDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['USB Hardware Design'],
            ['Generated by wasilzafar.com USB Development Mastery Series'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Target MCU', data.targetMcu || '', ''],
            ['Connector Type', data.connectorType || '', 'USB-A / USB-C / Micro-B / Mini-B'],
            ['USB Speed', data.usbSpeed || '', 'FS / HS'],
            ['PCB Layers', data.pcbLayers || '', '2, 4, or 6 layer'],
            ['ESD Protection', data.esdProtection || '', 'e.g. USBLC6-2SC6'],
            ['Polyfuse', data.polyfuse || '', 'e.g. MF-MSMF050'],
            ['Clock Source', data.clockSource || '', 'Crystal / HSE / Internal PLL'],
            ['Notes', data.notes || '', ''],
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'USB HW Design');
        XLSX.writeFile(wb, (filename || 'usb-hardware-design') + '.xlsx');
    },

    generateUsbHardwareDesignPDF: function(filename, data) {
        var lines = [
            { text: 'USB HARDWARE DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  MCU: ' + (data.targetMcu || 'N/A'), size: 12 },
            { text: 'Connector: ' + (data.connectorType || 'N/A') + '  |  Speed: ' + (data.usbSpeed || 'N/A') + '  |  Layers: ' + (data.pcbLayers || 'N/A'), size: 10 },
            { text: 'ESD: ' + (data.esdProtection || 'N/A') + '  |  Polyfuse: ' + (data.polyfuse || 'N/A') + '  |  Clock: ' + (data.clockSource || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── NOTES ──', size: 14, bold: true },
            { text: data.notes || 'None', size: 10 },
            { text: ' ', size: 6 },
            { text: '── NEXT STEPS ──', size: 14, bold: true },
            { text: 'Route 90 Ω diff pairs. Add TVS and polyfuse to BOM. Run SI simulation for HS USB.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'USB Hardware Design', lines: lines });
    },

    generateUsbHardwareDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'USB Hardware Design',
            subtitle: 'MCU: ' + (data.targetMcu || 'N/A') + '  |  ' + (data.connectorType || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Hardware Overview', content: 'MCU: ' + (data.targetMcu || 'N/A') + '\nConnector: ' + (data.connectorType || 'N/A') + '\nSpeed: ' + (data.usbSpeed || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'PCB & Protection', content: 'PCB Layers: ' + (data.pcbLayers || 'N/A') + '\nESD: ' + (data.esdProtection || 'N/A') + '\nPolyfuse: ' + (data.polyfuse || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Clock & Notes', content: 'Clock: ' + (data.clockSource || 'N/A') + '\n\n' + (data.notes || 'Not specified'), color: DocStyles.colors.blue },
                { heading: 'Next Steps', content: 'Route 90 Ω differential pairs.\nAdd TVS and polyfuse to BOM.\nRun SI simulation for HS USB.', color: DocStyles.colors.navy }
            ]
        });
    }

});
