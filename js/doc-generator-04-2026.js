/**
 * doc-generator-04-2026.js
 * Document generation methods for standalone technology articles.
 * Extends DocGenerator (doc-generator-core.js) via Object.assign.
 * 11 document types × 4 formats = 44 methods.
 *
 * @author wasilzafar.com
 */

Object.assign(DocGenerator, {

    // ============================================================
    // 1. GIT WORKFLOW ASSESSMENT
    // ============================================================

    generateGitWorkflowWord: async function(filename, data) {
        var sections = [
            { heading: 'Git Workflow Assessment', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Repository URL: ' + (data.repoUrl || 'N/A'),
                'Branch Strategy: ' + (data.branchStrategy || 'N/A'),
                'CI/CD Tool: ' + (data.ciCdTool || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Repository Overview', content: [
                'Repository: ' + (data.repoUrl || 'N/A'),
                'Primary branch strategy: ' + (data.branchStrategy || 'N/A'),
                'CI/CD integration: ' + (data.ciCdTool || 'N/A')
            ]},
            { heading: '2. Branching Strategy', content: (data.branchStrategy || 'Not specified').split('\n') },
            { heading: '3. Merge & Review Policy', content: (data.mergePolicy || 'Not specified').split('\n') },
            { heading: '4. CI/CD Integration', content: (data.ciCdTool || 'Not specified').split('\n') },
            { heading: '5. Git Hooks', content: (data.hookConfig || 'Not specified').split('\n') },
            { heading: '6. Recommendations', content: (data.notes || 'Enforce branch protection rules, require PR reviews, and automate linting via pre-commit hooks.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Git Workflow Assessment – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateGitWorkflowExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Git Workflow Assessment'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Repository URL', data.repoUrl || '', 'HTTPS or SSH clone URL'],
            ['Branch Strategy', data.branchStrategy || '', 'e.g. GitFlow, trunk-based, GitHub Flow'],
            ['Merge Policy', data.mergePolicy || '', 'Squash, rebase, or merge commit'],
            ['CI/CD Tool', data.ciCdTool || '', 'e.g. GitHub Actions, Jenkins, GitLab CI'],
            ['Hook Configuration', data.hookConfig || '', 'pre-commit, pre-push, commit-msg'],
            ['Required Reviewers', '', 'Minimum number of approvals'],
            ['Branch Protection', '', 'Enforce status checks before merge'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Git Workflow');
        XLSX.writeFile(wb, (filename || 'git-workflow-assessment') + '.xlsx');
    },

    generateGitWorkflowPDF: function(filename, data) {
        var lines = [
            { text: 'GIT WORKFLOW ASSESSMENT', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  ' + (data.repoUrl || 'N/A'), size: 12 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── REPOSITORY OVERVIEW ──', size: 14, bold: true },
            { text: 'Repository: ' + (data.repoUrl || 'N/A'), size: 10 },
            { text: 'CI/CD Tool: ' + (data.ciCdTool || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── BRANCHING STRATEGY ──', size: 14, bold: true },
            { text: data.branchStrategy || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── MERGE & REVIEW POLICY ──', size: 14, bold: true },
            { text: data.mergePolicy || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── GIT HOOKS ──', size: 14, bold: true },
            { text: data.hookConfig || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── RECOMMENDATIONS ──', size: 14, bold: true },
            { text: data.notes || 'Enforce branch protection and automate quality gates.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Git Workflow Assessment', lines: lines });
    },

    generateGitWorkflowPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Git Workflow Assessment',
            subtitle: (data.branchStrategy || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Repository Overview', content: 'Repo: ' + (data.repoUrl || 'N/A') + '\nBranch Strategy: ' + (data.branchStrategy || 'N/A') + '\nCI/CD: ' + (data.ciCdTool || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Merge & Review Policy', content: data.mergePolicy || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Git Hooks', content: data.hookConfig || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Recommendations', content: data.notes || 'Enforce branch protection rules.\nRequire PR reviews before merge.\nAutomate linting via pre-commit hooks.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 2. REST API DESIGN DOCUMENT
    // ============================================================

    generateApiDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'REST API Design Document', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'API Version: ' + (data.apiVersion || 'N/A'),
                'Base URL: ' + (data.baseUrl || 'N/A'),
                'Auth Method: ' + (data.authMethod || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. API Overview', content: [
                'Version: ' + (data.apiVersion || 'N/A'),
                'Base URL: ' + (data.baseUrl || 'N/A'),
                'Authentication: ' + (data.authMethod || 'N/A')
            ]},
            { heading: '2. Authentication', content: (data.authMethod || 'Not specified').split('\n') },
            { heading: '3. Endpoint Design', content: (data.endpoints || 'Not specified').split('\n') },
            { heading: '4. Rate Limiting & Throttling', content: (data.rateLimiting || 'Not specified').split('\n') },
            { heading: '5. Error Handling', content: (data.errorFormat || 'Not specified').split('\n') },
            { heading: '6. Documentation Plan', content: (data.notes || 'Publish OpenAPI 3.0 spec, maintain Postman collection, and auto-generate SDK clients.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'REST API Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateApiDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['REST API Design Document'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['API Version', data.apiVersion || '', 'e.g. v1, v2'],
            ['Base URL', data.baseUrl || '', 'e.g. https://api.example.com/v1'],
            ['Auth Method', data.authMethod || '', 'Bearer token, API key, OAuth 2.0'],
            ['Endpoints', data.endpoints || '', 'List of resource endpoints'],
            ['Rate Limiting', data.rateLimiting || '', 'e.g. 1000 req/min per API key'],
            ['Error Format', data.errorFormat || '', 'e.g. RFC 7807 Problem Details'],
            ['Content Type', '', 'application/json'],
            ['Versioning Strategy', '', 'URL path, header, or query param'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'API Design');
        XLSX.writeFile(wb, (filename || 'api-design-document') + '.xlsx');
    },

    generateApiDesignPDF: function(filename, data) {
        var lines = [
            { text: 'REST API DESIGN DOCUMENT', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Version: ' + (data.apiVersion || 'N/A'), size: 12 },
            { text: 'Base URL: ' + (data.baseUrl || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── AUTHENTICATION ──', size: 14, bold: true },
            { text: 'Method: ' + (data.authMethod || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── ENDPOINT DESIGN ──', size: 14, bold: true },
            { text: data.endpoints || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── RATE LIMITING ──', size: 14, bold: true },
            { text: data.rateLimiting || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── ERROR HANDLING ──', size: 14, bold: true },
            { text: data.errorFormat || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── DOCUMENTATION PLAN ──', size: 14, bold: true },
            { text: data.notes || 'Publish OpenAPI 3.0 spec and maintain Postman collection.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'REST API Design', lines: lines });
    },

    generateApiDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'REST API Design Document',
            subtitle: 'Version: ' + (data.apiVersion || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'API Overview', content: 'Base URL: ' + (data.baseUrl || 'N/A') + '\nAuth: ' + (data.authMethod || 'N/A') + '\nVersion: ' + (data.apiVersion || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Endpoint Design', content: data.endpoints || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Rate Limiting & Error Handling', content: 'Rate Limit: ' + (data.rateLimiting || 'N/A') + '\nError Format: ' + (data.errorFormat || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Documentation Plan', content: data.notes || 'Publish OpenAPI 3.0 spec.\nMaintain Postman collection.\nAuto-generate SDK clients.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 3. LINUX COMMAND REFERENCE CARD
    // ============================================================

    generateLinuxCmdWord: async function(filename, data) {
        var sections = [
            { heading: 'Linux Command Reference Card', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Distribution: ' + (data.distro || 'N/A'),
                'Shell Type: ' + (data.shellType || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. System Overview', content: [
                'Distribution: ' + (data.distro || 'N/A'),
                'Shell: ' + (data.shellType || 'N/A')
            ]},
            { heading: '2. Common Commands', content: (data.commonTasks || 'Not specified').split('\n') },
            { heading: '3. Shell Scripting', content: (data.scriptSnippets || 'Not specified').split('\n') },
            { heading: '4. Process & Service Management', content: [
                'Use systemctl for service management.',
                'Monitor processes with htop, top, or ps aux.',
                'Kill processes with kill -9 <PID> or pkill <name>.'
            ]},
            { heading: '5. Cron Jobs', content: (data.cronJobs || 'Not specified').split('\n') },
            { heading: '6. Security Hardening', content: (data.notes || 'Disable root SSH login, configure UFW firewall, and enable unattended-upgrades.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Linux Command Reference – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateLinuxCmdExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Linux Command Reference Card'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Distribution', data.distro || '', 'e.g. Ubuntu 22.04, CentOS 9, Arch'],
            ['Shell Type', data.shellType || '', 'e.g. bash, zsh, fish'],
            ['Common Tasks', data.commonTasks || '', 'File ops, networking, permissions'],
            ['Script Snippets', data.scriptSnippets || '', 'Reusable shell functions'],
            ['Cron Jobs', data.cronJobs || '', 'Scheduled task definitions'],
            ['Package Manager', '', 'apt, yum, pacman, dnf'],
            ['Firewall Config', '', 'ufw, iptables, firewalld'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Linux Commands');
        XLSX.writeFile(wb, (filename || 'linux-command-reference') + '.xlsx');
    },

    generateLinuxCmdPDF: function(filename, data) {
        var lines = [
            { text: 'LINUX COMMAND REFERENCE CARD', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Distro: ' + (data.distro || 'N/A'), size: 12 },
            { text: 'Shell: ' + (data.shellType || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── COMMON COMMANDS ──', size: 14, bold: true },
            { text: data.commonTasks || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SHELL SCRIPTING ──', size: 14, bold: true },
            { text: data.scriptSnippets || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PROCESS MANAGEMENT ──', size: 14, bold: true },
            { text: 'systemctl, htop, top, ps aux, kill, pkill', size: 10 },
            { text: ' ', size: 6 },
            { text: '── CRON JOBS ──', size: 14, bold: true },
            { text: data.cronJobs || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SECURITY HARDENING ──', size: 14, bold: true },
            { text: data.notes || 'Disable root SSH, configure firewall, enable auto-updates.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Linux Command Reference', lines: lines });
    },

    generateLinuxCmdPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Linux Command Reference Card',
            subtitle: (data.distro || 'N/A') + '  |  Shell: ' + (data.shellType || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'System Overview', content: 'Distribution: ' + (data.distro || 'N/A') + '\nShell: ' + (data.shellType || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Common Commands', content: data.commonTasks || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Shell Scripting & Cron', content: 'Scripts: ' + (data.scriptSnippets || 'N/A') + '\nCron: ' + (data.cronJobs || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Security Hardening', content: data.notes || 'Disable root SSH login.\nConfigure UFW firewall.\nEnable unattended-upgrades.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 4. REGEX PATTERN REFERENCE
    // ============================================================

    generateRegexRefWord: async function(filename, data) {
        var sections = [
            { heading: 'Regex Pattern Reference', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Language: ' + (data.language || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Pattern Overview', content: [
                'Target Language: ' + (data.language || 'N/A'),
                'Total patterns documented: see Core Patterns section.'
            ]},
            { heading: '2. Core Patterns', content: (data.patterns || 'Not specified').split('\n') },
            { heading: '3. Lookaheads & Lookbehinds', content: [
                'Positive lookahead: (?=pattern)',
                'Negative lookahead: (?!pattern)',
                'Positive lookbehind: (?<=pattern)',
                'Negative lookbehind: (?<!pattern)'
            ]},
            { heading: '4. Test Cases', content: (data.testCases || 'Not specified').split('\n') },
            { heading: '5. Validation Rules', content: (data.validationRules || 'Not specified').split('\n') },
            { heading: '6. Quick Reference', content: (data.notes || 'Use non-capturing groups (?:) when possible. Prefer possessive quantifiers for performance. Always test with edge cases.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Regex Pattern Reference – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateRegexRefExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Regex Pattern Reference'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Language', data.language || '', 'e.g. JavaScript, Python, Java, PCRE'],
            ['Patterns', data.patterns || '', 'Core regex patterns used'],
            ['Test Cases', data.testCases || '', 'Sample input/output pairs'],
            ['Validation Rules', data.validationRules || '', 'Email, URL, phone, etc.'],
            ['Lookahead', '(?=pattern)', 'Zero-width positive lookahead'],
            ['Lookbehind', '(?<=pattern)', 'Zero-width positive lookbehind'],
            ['Non-capturing Group', '(?:pattern)', 'Group without capturing'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Regex Reference');
        XLSX.writeFile(wb, (filename || 'regex-pattern-reference') + '.xlsx');
    },

    generateRegexRefPDF: function(filename, data) {
        var lines = [
            { text: 'REGEX PATTERN REFERENCE', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Language: ' + (data.language || 'N/A'), size: 12 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── CORE PATTERNS ──', size: 14, bold: true },
            { text: data.patterns || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── LOOKAHEADS & LOOKBEHINDS ──', size: 14, bold: true },
            { text: '(?=pat) lookahead  |  (?!pat) neg lookahead', size: 10 },
            { text: '(?<=pat) lookbehind  |  (?<!pat) neg lookbehind', size: 10 },
            { text: ' ', size: 6 },
            { text: '── TEST CASES ──', size: 14, bold: true },
            { text: data.testCases || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── VALIDATION RULES ──', size: 14, bold: true },
            { text: data.validationRules || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── QUICK REFERENCE ──', size: 14, bold: true },
            { text: data.notes || 'Use non-capturing groups, test edge cases, prefer possessive quantifiers.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Regex Pattern Reference', lines: lines });
    },

    generateRegexRefPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Regex Pattern Reference',
            subtitle: 'Language: ' + (data.language || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Core Patterns', content: data.patterns || 'Not specified', color: DocStyles.colors.navy },
                { heading: 'Lookaheads & Lookbehinds', content: '(?=pat) positive lookahead\n(?!pat) negative lookahead\n(?<=pat) positive lookbehind\n(?<!pat) negative lookbehind', color: DocStyles.colors.teal },
                { heading: 'Test Cases & Validation', content: 'Tests: ' + (data.testCases || 'N/A') + '\nValidation: ' + (data.validationRules || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Quick Reference', content: data.notes || 'Use non-capturing groups (?:).\nPrefer possessive quantifiers.\nAlways test with edge cases.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 5. AUTHENTICATION ARCHITECTURE DOCUMENT
    // ============================================================

    generateAuthDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'Authentication Architecture', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Auth Provider: ' + (data.authProvider || 'N/A'),
                'Flow Type: ' + (data.flowType || 'N/A'),
                'Token Lifetime: ' + (data.tokenLifetime || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Auth Overview', content: [
                'Provider: ' + (data.authProvider || 'N/A'),
                'Flow: ' + (data.flowType || 'N/A'),
                'Token Lifetime: ' + (data.tokenLifetime || 'N/A')
            ]},
            { heading: '2. OAuth 2.0 Flow Selection', content: (data.flowType || 'Not specified').split('\n') },
            { heading: '3. Token Management', content: [
                'Access Token Lifetime: ' + (data.tokenLifetime || 'N/A'),
                'Refresh Strategy: ' + (data.refreshStrategy || 'N/A'),
                'Storage: Secure HTTP-only cookies or encrypted local storage.'
            ]},
            { heading: '4. Refresh Strategy', content: (data.refreshStrategy || 'Not specified').split('\n') },
            { heading: '5. Scope & Permissions', content: (data.scopePermissions || 'Not specified').split('\n') },
            { heading: '6. Security Checklist', content: (data.notes || 'Enforce HTTPS, validate redirect URIs, rotate signing keys, implement PKCE for public clients.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Auth Architecture – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateAuthDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Authentication Architecture'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Auth Provider', data.authProvider || '', 'e.g. Auth0, Okta, AWS Cognito, Firebase'],
            ['Flow Type', data.flowType || '', 'Authorization Code, PKCE, Client Credentials'],
            ['Token Lifetime', data.tokenLifetime || '', 'e.g. 15 min access, 7 day refresh'],
            ['Refresh Strategy', data.refreshStrategy || '', 'Sliding window, absolute expiry, rotation'],
            ['Scope & Permissions', data.scopePermissions || '', 'read:users, write:data, admin:all'],
            ['Token Storage', '', 'HTTP-only cookie, encrypted storage'],
            ['PKCE Enabled', '', 'Required for public/SPA clients'],
            ['MFA Requirement', '', 'Optional, required for admin roles'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Auth Architecture');
        XLSX.writeFile(wb, (filename || 'auth-architecture') + '.xlsx');
    },

    generateAuthDesignPDF: function(filename, data) {
        var lines = [
            { text: 'AUTHENTICATION ARCHITECTURE', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Provider: ' + (data.authProvider || 'N/A'), size: 12 },
            { text: 'Flow: ' + (data.flowType || 'N/A') + '  |  Token TTL: ' + (data.tokenLifetime || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── OAUTH 2.0 FLOW ──', size: 14, bold: true },
            { text: data.flowType || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── TOKEN MANAGEMENT ──', size: 14, bold: true },
            { text: 'Lifetime: ' + (data.tokenLifetime || 'N/A') + '  |  Refresh: ' + (data.refreshStrategy || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── SCOPE & PERMISSIONS ──', size: 14, bold: true },
            { text: data.scopePermissions || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SECURITY CHECKLIST ──', size: 14, bold: true },
            { text: 'Enforce HTTPS for all auth endpoints.', size: 10 },
            { text: 'Validate redirect URIs. Rotate signing keys regularly.', size: 10 },
            { text: data.notes || 'Implement PKCE for public clients.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Auth Architecture', lines: lines });
    },

    generateAuthDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Authentication Architecture',
            subtitle: (data.authProvider || 'N/A') + '  |  ' + (data.flowType || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Auth Overview', content: 'Provider: ' + (data.authProvider || 'N/A') + '\nFlow: ' + (data.flowType || 'N/A') + '\nToken Lifetime: ' + (data.tokenLifetime || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Token & Refresh Strategy', content: 'Refresh: ' + (data.refreshStrategy || 'N/A') + '\nStorage: Secure HTTP-only cookies', color: DocStyles.colors.teal },
                { heading: 'Scope & Permissions', content: data.scopePermissions || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Security Checklist', content: data.notes || 'Enforce HTTPS.\nValidate redirect URIs.\nRotate signing keys.\nImplement PKCE for public clients.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 6. GRAPHQL SCHEMA DESIGN DOCUMENT
    // ============================================================

    generateGraphqlDesignWord: async function(filename, data) {
        var sections = [
            { heading: 'GraphQL Schema Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Schema Version: ' + (data.schemaVersion || 'N/A'),
                'Resolver Strategy: ' + (data.resolverStrategy || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Schema Overview', content: [
                'Version: ' + (data.schemaVersion || 'N/A'),
                'Resolver Strategy: ' + (data.resolverStrategy || 'N/A')
            ]},
            { heading: '2. Query Design', content: (data.queryTypes || 'Not specified').split('\n') },
            { heading: '3. Mutations', content: (data.mutationTypes || 'Not specified').split('\n') },
            { heading: '4. Subscriptions', content: (data.subscriptionTypes || 'Not specified').split('\n') },
            { heading: '5. Resolver Architecture', content: (data.resolverStrategy || 'Not specified').split('\n') },
            { heading: '6. Performance & Caching', content: (data.notes || 'Use DataLoader for N+1 prevention, enable persisted queries, implement field-level caching with TTLs.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'GraphQL Schema Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateGraphqlDesignExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['GraphQL Schema Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Schema Version', data.schemaVersion || '', 'e.g. 1.0, 2.0-beta'],
            ['Query Types', data.queryTypes || '', 'Root queries exposed to clients'],
            ['Mutation Types', data.mutationTypes || '', 'Write operations'],
            ['Subscription Types', data.subscriptionTypes || '', 'Real-time event subscriptions'],
            ['Resolver Strategy', data.resolverStrategy || '', 'DataLoader, batching, caching'],
            ['Depth Limit', '', 'Max query depth (e.g. 10)'],
            ['Complexity Limit', '', 'Max query cost (e.g. 1000)'],
            ['Persisted Queries', '', 'Enabled / Disabled'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'GraphQL Design');
        XLSX.writeFile(wb, (filename || 'graphql-schema-design') + '.xlsx');
    },

    generateGraphqlDesignPDF: function(filename, data) {
        var lines = [
            { text: 'GRAPHQL SCHEMA DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Schema: ' + (data.schemaVersion || 'N/A'), size: 12 },
            { text: 'Resolver Strategy: ' + (data.resolverStrategy || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── QUERY DESIGN ──', size: 14, bold: true },
            { text: data.queryTypes || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── MUTATIONS ──', size: 14, bold: true },
            { text: data.mutationTypes || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SUBSCRIPTIONS ──', size: 14, bold: true },
            { text: data.subscriptionTypes || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── RESOLVER ARCHITECTURE ──', size: 14, bold: true },
            { text: data.resolverStrategy || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PERFORMANCE & CACHING ──', size: 14, bold: true },
            { text: data.notes || 'Use DataLoader for N+1 prevention. Enable persisted queries.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'GraphQL Schema Design', lines: lines });
    },

    generateGraphqlDesignPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'GraphQL Schema Design',
            subtitle: 'Schema: ' + (data.schemaVersion || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Schema Overview', content: 'Version: ' + (data.schemaVersion || 'N/A') + '\nResolver: ' + (data.resolverStrategy || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Queries & Mutations', content: 'Queries: ' + (data.queryTypes || 'N/A') + '\nMutations: ' + (data.mutationTypes || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Subscriptions', content: data.subscriptionTypes || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Performance & Caching', content: data.notes || 'Use DataLoader for N+1.\nEnable persisted queries.\nSet field-level cache TTLs.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 7. RASPBERRY PI PROJECT PLAN
    // ============================================================

    generateRpiProjectWord: async function(filename, data) {
        var sections = [
            { heading: 'Raspberry Pi Project Plan', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'RPi Model: ' + (data.rpiModel || 'N/A'),
                'OS Choice: ' + (data.osChoice || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Project Overview', content: [
                'Model: ' + (data.rpiModel || 'N/A'),
                'Operating System: ' + (data.osChoice || 'N/A'),
                'Power Requirements: ' + (data.powerRequirements || 'N/A')
            ]},
            { heading: '2. Hardware Selection', content: [
                'RPi Model: ' + (data.rpiModel || 'N/A'),
                'Peripherals: ' + (data.peripherals || 'N/A'),
                'Power Supply: ' + (data.powerRequirements || 'N/A')
            ]},
            { heading: '3. GPIO Pin Assignment', content: (data.gpioUsage || 'Not specified').split('\n') },
            { heading: '4. Peripheral Integration', content: (data.peripherals || 'Not specified').split('\n') },
            { heading: '5. Power Design', content: (data.powerRequirements || 'Not specified').split('\n') },
            { heading: '6. Software Stack', content: (data.notes || 'Install Raspberry Pi OS Lite, configure I2C/SPI interfaces, set up Python virtual environment with GPIO libraries.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'RPi Project Plan – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateRpiProjectExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Raspberry Pi Project Plan'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['RPi Model', data.rpiModel || '', 'e.g. RPi 4B 4GB, RPi 5, RPi Zero 2W'],
            ['OS Choice', data.osChoice || '', 'e.g. Raspberry Pi OS Lite, Ubuntu Server'],
            ['GPIO Usage', data.gpioUsage || '', 'Pin assignments for I2C, SPI, UART'],
            ['Peripherals', data.peripherals || '', 'Sensors, displays, relays, cameras'],
            ['Power Requirements', data.powerRequirements || '', 'e.g. 5V 3A USB-C supply'],
            ['Storage', '', 'SD card size and class'],
            ['Network Config', '', 'WiFi, Ethernet, static IP'],
            ['Enclosure', '', 'Case type and mounting'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'RPi Project');
        XLSX.writeFile(wb, (filename || 'rpi-project-plan') + '.xlsx');
    },

    generateRpiProjectPDF: function(filename, data) {
        var lines = [
            { text: 'RASPBERRY PI PROJECT PLAN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Model: ' + (data.rpiModel || 'N/A'), size: 12 },
            { text: 'OS: ' + (data.osChoice || 'N/A') + '  |  Power: ' + (data.powerRequirements || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── HARDWARE SELECTION ──', size: 14, bold: true },
            { text: 'Model: ' + (data.rpiModel || 'N/A'), size: 10 },
            { text: 'Peripherals: ' + (data.peripherals || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── GPIO PIN ASSIGNMENT ──', size: 14, bold: true },
            { text: data.gpioUsage || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PERIPHERAL INTEGRATION ──', size: 14, bold: true },
            { text: data.peripherals || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── POWER DESIGN ──', size: 14, bold: true },
            { text: data.powerRequirements || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SOFTWARE STACK ──', size: 14, bold: true },
            { text: data.notes || 'RPi OS Lite, Python GPIO, I2C/SPI enabled.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'RPi Project Plan', lines: lines });
    },

    generateRpiProjectPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Raspberry Pi Project Plan',
            subtitle: (data.rpiModel || 'N/A') + '  |  ' + (data.osChoice || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Project Overview', content: 'Model: ' + (data.rpiModel || 'N/A') + '\nOS: ' + (data.osChoice || 'N/A') + '\nPower: ' + (data.powerRequirements || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'GPIO & Peripherals', content: 'GPIO: ' + (data.gpioUsage || 'N/A') + '\nPeripherals: ' + (data.peripherals || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Power Design', content: data.powerRequirements || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Software Stack', content: data.notes || 'Raspberry Pi OS Lite.\nPython + RPi.GPIO / gpiozero.\nEnable I2C, SPI, UART interfaces.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 8. ESP32 IOT PROJECT PLAN
    // ============================================================

    generateEsp32ProjectWord: async function(filename, data) {
        var sections = [
            { heading: 'ESP32 IoT Project Plan', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'ESP32 Model: ' + (data.esp32Model || 'N/A'),
                'Connectivity: ' + (data.connectivity || 'N/A'),
                'MQTT Broker: ' + (data.mqttBroker || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Project Overview', content: [
                'Model: ' + (data.esp32Model || 'N/A'),
                'Connectivity: ' + (data.connectivity || 'N/A'),
                'Power Mode: ' + (data.powerMode || 'N/A')
            ]},
            { heading: '2. Hardware Config', content: [
                'ESP32 Model: ' + (data.esp32Model || 'N/A'),
                'Sensors: ' + (data.sensorList || 'N/A'),
                'Power Mode: ' + (data.powerMode || 'N/A')
            ]},
            { heading: '3. Connectivity (WiFi/BLE)', content: (data.connectivity || 'Not specified').split('\n') },
            { heading: '4. MQTT & Cloud', content: (data.mqttBroker || 'Not specified').split('\n') },
            { heading: '5. Sensors & Actuators', content: (data.sensorList || 'Not specified').split('\n') },
            { heading: '6. Power Management', content: (data.powerMode || 'Not specified').split('\n') },
            { heading: '7. OTA Updates', content: (data.otaStrategy || 'Not specified').split('\n') }
        ];
        return this.generateWord(filename, { title: 'ESP32 IoT Project – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateEsp32ProjectExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['ESP32 IoT Project Plan'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['ESP32 Model', data.esp32Model || '', 'e.g. ESP32-S3, ESP32-C3, ESP32-WROOM'],
            ['Connectivity', data.connectivity || '', 'WiFi 2.4 GHz, BLE 5.0, ESP-NOW'],
            ['MQTT Broker', data.mqttBroker || '', 'e.g. Mosquitto, AWS IoT Core, HiveMQ'],
            ['Sensor List', data.sensorList || '', 'Temperature, humidity, motion, etc.'],
            ['Power Mode', data.powerMode || '', 'Active, light sleep, deep sleep'],
            ['OTA Strategy', data.otaStrategy || '', 'HTTP OTA, MQTT-triggered, rollback'],
            ['Flash Size', '', 'e.g. 4MB, 8MB, 16MB'],
            ['Partition Scheme', '', 'Factory + OTA0 + OTA1'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'ESP32 Project');
        XLSX.writeFile(wb, (filename || 'esp32-iot-project') + '.xlsx');
    },

    generateEsp32ProjectPDF: function(filename, data) {
        var lines = [
            { text: 'ESP32 IOT PROJECT PLAN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Model: ' + (data.esp32Model || 'N/A'), size: 12 },
            { text: 'Connectivity: ' + (data.connectivity || 'N/A') + '  |  Power: ' + (data.powerMode || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── HARDWARE CONFIG ──', size: 14, bold: true },
            { text: 'Model: ' + (data.esp32Model || 'N/A') + '  |  Sensors: ' + (data.sensorList || 'N/A'), size: 10 },
            { text: ' ', size: 6 },
            { text: '── CONNECTIVITY ──', size: 14, bold: true },
            { text: data.connectivity || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── MQTT & CLOUD ──', size: 14, bold: true },
            { text: data.mqttBroker || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SENSORS & ACTUATORS ──', size: 14, bold: true },
            { text: data.sensorList || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── POWER MANAGEMENT ──', size: 14, bold: true },
            { text: data.powerMode || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── OTA UPDATES ──', size: 14, bold: true },
            { text: data.otaStrategy || 'Not specified', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'ESP32 IoT Project', lines: lines });
    },

    generateEsp32ProjectPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'ESP32 IoT Project Plan',
            subtitle: (data.esp32Model || 'N/A') + '  |  ' + (data.connectivity || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Hardware & Connectivity', content: 'Model: ' + (data.esp32Model || 'N/A') + '\nConnectivity: ' + (data.connectivity || 'N/A') + '\nSensors: ' + (data.sensorList || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'MQTT & Cloud', content: 'Broker: ' + (data.mqttBroker || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Power Management', content: 'Mode: ' + (data.powerMode || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'OTA Updates', content: data.otaStrategy || 'Not specified', color: DocStyles.colors.navy },
                { heading: 'Notes', content: data.notes || 'Configure partition scheme for dual OTA.\nImplement deep sleep for battery operation.\nUse TLS for MQTT connections.', color: DocStyles.colors.teal }
            ]
        });
    },

    // ============================================================
    // 9. SECURE BOOT SECURITY AUDIT
    // ============================================================

    generateSecBootAuditWord: async function(filename, data) {
        var sections = [
            { heading: 'Secure Boot Security Audit', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Platform: ' + (data.platform || 'N/A'),
                'Key Storage: ' + (data.keyStorage || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Platform Overview', content: [
                'Platform: ' + (data.platform || 'N/A'),
                'Boot Chain: ' + (data.bootChain || 'N/A'),
                'Key Storage: ' + (data.keyStorage || 'N/A')
            ]},
            { heading: '2. Boot Chain Analysis', content: (data.bootChain || 'Not specified').split('\n') },
            { heading: '3. Key Management', content: (data.keyStorage || 'Not specified').split('\n') },
            { heading: '4. Firmware Signing', content: (data.firmwareSigning || 'Not specified').split('\n') },
            { heading: '5. Tamper Detection', content: (data.tamperDetection || 'Not specified').split('\n') },
            { heading: '6. Compliance Checklist', content: (data.notes || 'Verify root of trust is hardware-based. Ensure key revocation is supported. Validate anti-rollback counters are implemented.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Secure Boot Audit – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateSecBootAuditExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Secure Boot Security Audit'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Platform', data.platform || '', 'e.g. ARM TrustZone, x86 UEFI, RISC-V'],
            ['Boot Chain', data.bootChain || '', 'ROM > BL1 > BL2 > BL31 > Kernel'],
            ['Key Storage', data.keyStorage || '', 'OTP fuses, TPM, HSM, eFuse'],
            ['Firmware Signing', data.firmwareSigning || '', 'RSA-2048, ECDSA P-256, Ed25519'],
            ['Tamper Detection', data.tamperDetection || '', 'Anti-debug, fuse locks, voltage glitch'],
            ['Anti-Rollback', '', 'Monotonic counter in OTP fuses'],
            ['Key Revocation', '', 'Supported / Not supported'],
            ['Root of Trust', '', 'Hardware-based / Software-based'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Secure Boot Audit');
        XLSX.writeFile(wb, (filename || 'secure-boot-audit') + '.xlsx');
    },

    generateSecBootAuditPDF: function(filename, data) {
        var lines = [
            { text: 'SECURE BOOT SECURITY AUDIT', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Platform: ' + (data.platform || 'N/A'), size: 12 },
            { text: 'Key Storage: ' + (data.keyStorage || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── BOOT CHAIN ANALYSIS ──', size: 14, bold: true },
            { text: data.bootChain || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── KEY MANAGEMENT ──', size: 14, bold: true },
            { text: data.keyStorage || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── FIRMWARE SIGNING ──', size: 14, bold: true },
            { text: data.firmwareSigning || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── TAMPER DETECTION ──', size: 14, bold: true },
            { text: data.tamperDetection || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── COMPLIANCE CHECKLIST ──', size: 14, bold: true },
            { text: data.notes || 'Verify hardware root of trust. Validate anti-rollback counters.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Secure Boot Audit', lines: lines });
    },

    generateSecBootAuditPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Secure Boot Security Audit',
            subtitle: (data.platform || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Platform Overview', content: 'Platform: ' + (data.platform || 'N/A') + '\nBoot Chain: ' + (data.bootChain || 'N/A') + '\nKey Storage: ' + (data.keyStorage || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Firmware Signing', content: data.firmwareSigning || 'Not specified', color: DocStyles.colors.teal },
                { heading: 'Tamper Detection', content: data.tamperDetection || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Compliance Checklist', content: data.notes || 'Hardware root of trust verified.\nAnti-rollback counters implemented.\nKey revocation supported.', color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 10. EMBEDDED INTERVIEW PREP SHEET
    // ============================================================

    generateEmbInterviewPrepWord: async function(filename, data) {
        var sections = [
            { heading: 'Embedded Interview Prep Sheet', content: [
                'Candidate Name: ' + (data.candidateName || 'N/A'),
                'Target Role: ' + (data.targetRole || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Candidate Profile', content: [
                'Name: ' + (data.candidateName || 'N/A'),
                'Target Role: ' + (data.targetRole || 'N/A')
            ]},
            { heading: '2. C/C++ & Memory', content: (data.cTopics || 'Not specified').split('\n') },
            { heading: '3. RTOS & Scheduling', content: (data.rtosTopics || 'Not specified').split('\n') },
            { heading: '4. Communication Protocols', content: (data.protocolTopics || 'Not specified').split('\n') },
            { heading: '5. Debugging & Tools', content: (data.debugTopics || 'Not specified').split('\n') },
            { heading: '6. Practice Problems', content: (data.notes || 'Implement a circular buffer in C. Write an ISR-safe FIFO queue. Explain priority inversion and its mitigations.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Embedded Interview Prep – ' + (data.candidateName || ''), author: data.candidateName || '', sections: sections });
    },

    generateEmbInterviewPrepExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Embedded Interview Prep Sheet'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Candidate Name', data.candidateName || '', ''],
            ['Target Role', data.targetRole || '', 'e.g. Firmware Engineer, Embedded SWE'],
            ['C/C++ Topics', data.cTopics || '', 'Pointers, volatile, memory layout, bit manipulation'],
            ['RTOS Topics', data.rtosTopics || '', 'FreeRTOS, Zephyr, task scheduling, semaphores'],
            ['Protocol Topics', data.protocolTopics || '', 'I2C, SPI, UART, CAN, USB, Ethernet'],
            ['Debug Topics', data.debugTopics || '', 'JTAG, SWD, logic analyzer, oscilloscope'],
            ['Data Structures', '', 'Circular buffer, linked list, state machine'],
            ['Interrupt Handling', '', 'ISR design, latency, nested interrupts'],
            ['Memory Management', '', 'Stack vs heap, memory pools, fragmentation'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Interview Prep');
        XLSX.writeFile(wb, (filename || 'embedded-interview-prep') + '.xlsx');
    },

    generateEmbInterviewPrepPDF: function(filename, data) {
        var lines = [
            { text: 'EMBEDDED INTERVIEW PREP SHEET', size: 18, bold: true },
            { text: (data.candidateName || '') + '  |  Role: ' + (data.targetRole || 'N/A'), size: 12 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── C/C++ & MEMORY ──', size: 14, bold: true },
            { text: data.cTopics || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── RTOS & SCHEDULING ──', size: 14, bold: true },
            { text: data.rtosTopics || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── COMMUNICATION PROTOCOLS ──', size: 14, bold: true },
            { text: data.protocolTopics || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── DEBUGGING & TOOLS ──', size: 14, bold: true },
            { text: data.debugTopics || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── PRACTICE PROBLEMS ──', size: 14, bold: true },
            { text: data.notes || 'Implement circular buffer. Write ISR-safe FIFO. Explain priority inversion.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Embedded Interview Prep', lines: lines });
    },

    generateEmbInterviewPrepPPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.candidateName || 'Candidate',
            title: 'Embedded Interview Prep Sheet',
            subtitle: 'Target: ' + (data.targetRole || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Candidate Profile', content: 'Name: ' + (data.candidateName || 'N/A') + '\nTarget Role: ' + (data.targetRole || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'C/C++ & Memory', content: data.cTopics || 'Pointers, volatile, memory layout, bit manipulation', color: DocStyles.colors.teal },
                { heading: 'RTOS & Protocols', content: 'RTOS: ' + (data.rtosTopics || 'N/A') + '\nProtocols: ' + (data.protocolTopics || 'N/A'), color: DocStyles.colors.blue },
                { heading: 'Debugging & Practice', content: 'Tools: ' + (data.debugTopics || 'N/A') + '\n' + (data.notes || 'Circular buffer, ISR-safe FIFO, priority inversion'), color: DocStyles.colors.navy }
            ]
        });
    },

    // ============================================================
    // 11. SPARK DATA PIPELINE DESIGN
    // ============================================================

    generateSparkPipelineWord: async function(filename, data) {
        var sections = [
            { heading: 'Spark Data Pipeline Design', content: [
                'Project Name: ' + (data.projectName || 'N/A'),
                'Cluster Size: ' + (data.clusterSize || 'N/A'),
                'Schedule: ' + (data.scheduleFreq || 'N/A'),
                'Date: ' + new Date().toLocaleDateString()
            ]},
            { heading: '1. Pipeline Overview', content: [
                'Cluster: ' + (data.clusterSize || 'N/A'),
                'Input Sources: ' + (data.inputSources || 'N/A'),
                'Output Sinks: ' + (data.outputSinks || 'N/A')
            ]},
            { heading: '2. Cluster Configuration', content: (data.clusterSize || 'Not specified').split('\n') },
            { heading: '3. Data Sources', content: (data.inputSources || 'Not specified').split('\n') },
            { heading: '4. Transformations', content: (data.transformations || 'Not specified').split('\n') },
            { heading: '5. Output Sinks', content: (data.outputSinks || 'Not specified').split('\n') },
            { heading: '6. Scheduling & Monitoring', content: (data.notes || 'Schedule via Airflow DAG. Monitor with Spark UI and Datadog. Set up alerting for job failures and SLA breaches.').split('\n') }
        ];
        return this.generateWord(filename, { title: 'Spark Pipeline Design – ' + (data.projectName || ''), author: data.projectName || '', sections: sections });
    },

    generateSparkPipelineExcel: function(filename, data) {
        var wb = XLSX.utils.book_new();
        var ws_data = [
            ['Spark Data Pipeline Design'],
            ['Generated by wasilzafar.com'],
            [],
            ['Field', 'Value', 'Notes'],
            ['Project Name', data.projectName || '', ''],
            ['Cluster Size', data.clusterSize || '', 'e.g. 3 workers × m5.2xlarge'],
            ['Input Sources', data.inputSources || '', 'S3, Kafka, JDBC, Delta Lake'],
            ['Transformations', data.transformations || '', 'Filter, join, aggregate, window'],
            ['Output Sinks', data.outputSinks || '', 'Delta Lake, Redshift, BigQuery, S3'],
            ['Schedule Frequency', data.scheduleFreq || '', 'e.g. hourly, daily, event-driven'],
            ['Spark Version', '', 'e.g. 3.5.x'],
            ['Executor Memory', '', 'e.g. 8g per executor'],
            ['Shuffle Partitions', '', 'Default 200, tune per workload'],
            ['Orchestrator', '', 'Airflow, Databricks Workflows, Step Functions'],
            ['Notes', data.notes || '', '']
        ];
        var ws = XLSX.utils.aoa_to_sheet(ws_data);
        ws['!cols'] = [{ wch: 24 }, { wch: 40 }, { wch: 36 }];
        XLSX.utils.book_append_sheet(wb, ws, 'Spark Pipeline');
        XLSX.writeFile(wb, (filename || 'spark-pipeline-design') + '.xlsx');
    },

    generateSparkPipelinePDF: function(filename, data) {
        var lines = [
            { text: 'SPARK DATA PIPELINE DESIGN', size: 18, bold: true },
            { text: (data.projectName || '') + '  |  Cluster: ' + (data.clusterSize || 'N/A'), size: 12 },
            { text: 'Schedule: ' + (data.scheduleFreq || 'N/A'), size: 10 },
            { text: 'Generated: ' + new Date().toLocaleDateString(), size: 9 },
            { text: ' ', size: 6 },
            { text: '── CLUSTER CONFIGURATION ──', size: 14, bold: true },
            { text: data.clusterSize || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── DATA SOURCES ──', size: 14, bold: true },
            { text: data.inputSources || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── TRANSFORMATIONS ──', size: 14, bold: true },
            { text: data.transformations || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── OUTPUT SINKS ──', size: 14, bold: true },
            { text: data.outputSinks || 'Not specified', size: 10 },
            { text: ' ', size: 6 },
            { text: '── SCHEDULING & MONITORING ──', size: 14, bold: true },
            { text: data.notes || 'Airflow DAG orchestration. Spark UI + Datadog monitoring.', size: 10 }
        ];
        return this.generatePDF(filename, { title: 'Spark Pipeline Design', lines: lines });
    },

    generateSparkPipelinePPTX: async function(filename, data) {
        return this._generateSectionsPPTX(filename, {
            entityName: data.projectName || 'Project',
            title: 'Spark Data Pipeline Design',
            subtitle: 'Cluster: ' + (data.clusterSize || 'N/A') + '  |  ' + new Date().toLocaleDateString(),
            perSlide: 2,
            sections: [
                { heading: 'Pipeline Overview', content: 'Cluster: ' + (data.clusterSize || 'N/A') + '\nSources: ' + (data.inputSources || 'N/A') + '\nSinks: ' + (data.outputSinks || 'N/A'), color: DocStyles.colors.navy },
                { heading: 'Data Sources & Transformations', content: 'Sources: ' + (data.inputSources || 'N/A') + '\nTransformations: ' + (data.transformations || 'N/A'), color: DocStyles.colors.teal },
                { heading: 'Output Sinks', content: data.outputSinks || 'Not specified', color: DocStyles.colors.blue },
                { heading: 'Scheduling & Monitoring', content: 'Frequency: ' + (data.scheduleFreq || 'N/A') + '\n' + (data.notes || 'Airflow DAG orchestration.\nSpark UI + Datadog monitoring.\nAlert on job failures and SLA breaches.'), color: DocStyles.colors.navy }
            ]
        });
    }

});
