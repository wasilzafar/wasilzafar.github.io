{
  "name": "og-image-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Generate unique Open Graph images for all blog articles",
  "scripts": {
    "build": "node build.js",
    "build:incremental": "node build.js --incremental"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.65",
    "sharp": "^0.33.2"
  }
}
